# Phase 8 (Module J) — archived rounds

Archived verbatim from `review.md` on 2026-08-24 when Phase 13's FULL round became the current round. Contains the 2026-08-22 session's Phase 8 sections ((A2) original checklist FULL round, (A) SalesmanNameReview queue amend) plus the same-session items (B)/(C) that were written into that round's summary and would otherwise be lost.

## (A2) Phase 8 original checklist — FULL round (first verification for these tasks), same session

Schema contract for all five Module J models matched design.md field-for-field; migration applied. DB invariants on the real dataset: **0** `SalesLine` without credit · **0** lines with `Σ sharePercent ≠ 100` · credited revenue total = company total exactly (14,555,965.12) — the contract's auto-check equation. Normalizer single-source confirmed by grep (every consumer imports `nameNormalizer.util`). Import behavior live-tested with real fixtures: new shared combination of two known people → rule auto-created at 50/50 (`WARNING SHARED_CREDIT_RULE_CREATED`) and line imported with both credits; shared deal containing an unknown spelling → `ERROR UNKNOWN_SALESMAN_IN_SHARED_DEAL`, row skipped, known person NOT credited (design item 5 letter); hospital variant whose `thaiCore` matches but `latinCore` differs → row imports as a NEW hospital (`NEW_HOSPITAL` warning) **and** `HospitalNameReview(PENDING)` queued with the sorted latinCore pair — never auto-merged (note: `thaiCore` keeps digits, so suffixes like "PHASE 9" legitimately do not collide). Endpoints: `GET /salesman-name-rules` (2 rules incl. the seeded Chokechai/Chidchanok 50/50), `PATCH /salesman-name-rules/:id` live-edited 50/50→60/40 then Σ≠100 rejected `400`; `GET/PATCH /hospital-name-reviews` live-tested via a throwaway PENDING pair decided KEPT_SEPARATE. Backfill script flags (idempotent/--revert) and one-time cleanup script (`--revert=<backup>`) read in full; cleanup's earlier documented run found zero duplicate pairs in this DB. Frontend: name-reviews page tabs + Thai badge labels for both new issue codes confirmed in `ImportIssueTable.tsx`. All test rows removed afterwards — final DB state: 10 salespeople, 141 hospitals, 37 KEPT_SEPARATE reviews intact, 1 legitimate rule. Non-blocking observation routed to `system-analyst`: shared deals whose members are ALL unseen error-skip (per item-5 letter) instead of auto-creating people like the single-name path — UX decision worth revisiting when Module J is next amended.

## (A) Phase 8 amend — SalesmanNameReview queue (first-ever verification for these tasks)

**(B) Phase 2 amend — `employmentEndedAt` editor, (C) Phase 11 — routed denormalization fix + new `PRODUCT_TYPE_ALIAS_MISMATCH` WARNING (TARGETED: a FULL round exists, archived to `review/phase-11.md`).** — same-session items, kept verbatim here because they were part of this round's write-up.

Read `_docs/status.md`, `plan.md` (Phase 8 amend block, Phase 2 amend block, Phase 11 block, Unresolved Open Questions #14), `design.md` (Data Model `SalesmanNameReview`, Import Rules salesman rule item 8, Product Master & Ranking Rules item 2's WARNING rule), and `requirement.md` OQ30/OQ20 before verifying.

**Schema/Data Model contract check**: `backend/prisma/schema.prisma`'s `SalesmanNameReview` matches `design.md` field-for-field (`personKey @unique`, `sampleRaw`, `status NameReviewStatus @default(PENDING)`, `createdSalespersonId? @unique` with `onDelete: SetNull`, `mergedIntoId?`/`mergedInto`, `decidedById`/`decidedBy`/`decidedAt`, `note`, `@@index([status])`) plus back-relations on `Salesperson` (two named relations) and `User`. Migration `20260822030000_phase8_salesman_name_review` applied to the real Supabase DB (`prisma migrate deploy` — "All migrations have been successfully applied"). Additive only; no existing table touched. Schema confirmed by the user before implementation (hard stop honored).

**Import integration — live-tested end to end with real `.xlsx` fixtures through real `POST /import` calls against the real dev server + DB** (dev server restarted first so it loaded the regenerated Prisma client):
1. Unknown salesman spelling ("Mr.QaReview Testone") → import SUCCESS, `WARNING UNKNOWN_SALESMAN` raised with the new queue-pointing message, `SalesmanNameReview(PENDING)` row created with `personKey = personCore(sampleRaw)` and linked `createdSalesperson`.
2. Re-import of the same file → `updatedRows: 1, insertedRows: 0`, **no duplicate person, no duplicate review, zero new issues** (resolves via personCore index).
3. `PATCH .../KEPT_SEPARATE` → status flips with `decidedAt`; re-deciding → `409`; re-import again still clean.
4. Second spelling ("Testtwo") → PENDING #2 → `PATCH MERGED {mergedIntoId}` → transaction moved its `SalesLine` + `SalesLineCredit` to the target person, deleted the duplicate `Salesperson` row (`createdSalesperson` reads null after), review records decision; **a third import of that exact spelling resolved via the MERGED memory straight to the target person — no warning, no re-ask** (the contract's "ตัดสินแล้วต้องไม่ถามซ้ำ").
5. Merge edge cases: self-point `mergedIntoId` → rejected (400), nonexistent target → `404`, both leaving the review untouched at PENDING.
6. Role gates: unauthenticated GET → `401`; active SALESPERSON token → `403` on `GET /salesman-name-reviews`, `403` on `PATCH /salespeople/:id` (employmentEndedAt), while open-read `GET /salespeople` stays `200`. (The previously-used `qa.temp.user@example.com` is inactive and returns 401 — not a valid role-gate probe; a throwaway active SALESPERSON account was created for this round and deleted afterwards.)

**(B) Phase 2 amend — `PATCH /salespeople/:id` now accepts `employmentEndedAt`**: live-tested success (`null` round-trip; date form validated as YYYY-MM-DD), invalid date → 400, empty body → 400 (refine), SALESPERSON → 403, MANAGER → 200. Frontend master-data page renders the date input per row (MANAGER-editable) with the "ไม่ปิด assignment อัตโนมัติ" note; visual viewport check remains covered by the standing responsive gap below.

**(C) Phase 11 fix proof — the exact bug scenario from the 2026-08-19 FULL round, live-reproduced then cleaned**: fixture imports aliased "PC-5" under file `Product type = "C-cae"` (real type GI). Result: row imported, **`WARNING PRODUCT_TYPE_ALIAS_MISMATCH` raised with a manager-readable message naming both types**, and direct DB query confirms `SalesLine.productTypeId == product.productTypeId (GI)` — the invariant at design.md line ~436 holds; the raw column value does not leak into the denormalization. This closes the ❌ item from the previous round.

**(A) Phase 8 original checklist scope note**: this round verified the *amend* tasks (queue model/import hook/endpoints/frontend tab). Phase 8's pre-existing backend checklist (backfill/cleanup scripts, hospital-name reviews, name rules) was implemented and self-live-tested on 2026-08-16 but has **never had its own verification round** — Phase 8 still needs a FULL QA round of its own before any `devops` eligibility claim. *(Closed the same day by the (A2) FULL round above.)*

All test data removed after each check; final DB state reconfirmed: 10 salespeople, 0 pending reviews, no leftover QA rows/accounts. Whole-project `typecheck`/`lint`/`build` green on both `backend`/`frontend`. No automated test suite exists — this round is code inspection plus real HTTP calls against a real dev server and the real Supabase DB.

## Change Log (Phase 8)

- 2026-08-24 — File created: 2026-08-22 round content moved here verbatim from `review.md` when Phase 13's FULL round replaced it as current. No content edited beyond this header and the bracketed closure note.
