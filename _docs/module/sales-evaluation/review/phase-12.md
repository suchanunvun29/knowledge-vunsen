# Phase 12 (Module M) — archived rounds

Archived verbatim from `review.md` on 2026-08-24 when Phase 13's FULL round became the current round. Contains the 2026-08-22 Phase 12 backend verification summary.

## Verification Summary (as recorded that round)

**(D) Phase 12 backend round (Module M) — same day.** Migration/schema matched the round-4 contract field-for-field (`isSupervisor Boolean` — banned enum absent; `btree_gist` exclusion constraint on member ranges; `TargetScope` 3 values; `Target` nullable owner + dual unique indexes). Region seed correct (5 แถว). All existing targets carry `scope = SALESPERSON`. Scope-guards present on every Target read (list/upsert/copy/kpi×3; drill-down omits the literal but XOR nullability makes cross-scope reads impossible). Live smoke through real endpoints: territory create/rename/list; assignment PUT + GET list filtered; group create/member with full-month Zod boundaries (`400`), duplicate/exclusion → **409**, membership covering a month that has a TERRITORY target → **409**, and PUT target on a grouped month → **409** (both guard directions proven); PUT territory-target happy path → 200 with revision snapshot; PUT group-target (14M shape) → 200; PATCH hospital territory with `HospitalTerritoryChange` audit trail verified row-for-row, then reverted; bulk-by-province wiring proven (`updatedCount=0` on an empty province); unassigned-territory list returns all 141 hospitals with bucket sums; derived target MANUAL-override path → correct personal figures. **Gap found & fixed in-round**: the contract's bootstrap script was missing entirely — implemented as `scripts/bootstrapTerritories.ts` per Territory KPI Rules item 9 (top credited spender excluding excluded-personnel, single-active-territory requirement, ≥30% runner-up ambiguity flag, MANUAL protection, idempotent, `--dry-run`) and dry-run-proven over all 141 hospitals (correctly infers nothing while no assignments exist: 134 "no active territory" flags + 7 genuine ≥30% ambiguity flags for manager decisions). Not yet covered this phase: frontend ×7 pages and a real bootstrap run (manager must create territories/assignments through the UI first — data work, not code).

## Change Log (Phase 12)

- 2026-08-24 — File created: the 2026-08-22 Phase 12 backend round moved here verbatim from `review.md`. The phase's frontend ×7 pages and the real bootstrap run remain outstanding (unchanged by the move).
