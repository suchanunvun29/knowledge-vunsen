# ระบบประเมินและสนับสนุนพนักงานขาย (Sales Evaluation & Enablement) — Design Archive

เอกสารนี้เก็บบันทึกการตัดสินใจ (คำถาม→คำตอบ→เหตุผล) ที่ **ปิดรอบแล้ว** ย้ายมาจาก `design.md` แบบคำต่อคำตาม `policies/documentation.md` §4 — กฎที่ได้จากการตัดสินใจแต่ละข้อยังอยู่ที่ Contract section / Data Model / `## Modules` ของ `design.md` ตามปกติ (ไม่มีอะไรถูกย้ายออกจากที่ที่มันบังคับใช้จริง) ไฟล์นี้เก็บไว้เฉพาะ **ที่มา/เหตุผลของกฎ** ที่ไม่จำเป็นต้องอ่านซ้ำทุกรอบอีกต่อไป

ไม่มีเนื้อหาในไฟล์นี้ถูกสรุปหรือตัดทอน — เป็นการย้ายคำต่อคำทั้งหมด

---

## Feature-by-Feature Feasibility — decision log (archived 2026-08-25)

ตารางการตัดสินใจ 5 ชุด (แถวที่ 1–32 รวมทุกรอบ) ที่เคยอยู่ใต้หัวข้อ `## Feature-by-Feature Feasibility` ใน `design.md` — ปิดครบทุกแถวแล้ว กฎที่ได้อยู่ใน Import Rules / KPI & Scoring Rules / Territory & Potential Rules / Data Visibility Rules / Territory KPI Rules / Data Model / `## Modules` ของ `design.md` ปัจจุบันทั้งหมด ตารางประเมินความเป็นไปได้ (ฟีเจอร์ / ผลประเมิน / หมายเหตุ) ของแต่ละรอบ **ไม่ได้ย้ายมาที่นี่** — ยังอยู่ใน `design.md` ตามเดิมเพราะเป็นเนื้อหาหลักของหัวข้อนี้ ไม่ใช่บันทึกการตัดสินใจ

**การตัดสินใจที่ผู้ใช้ยืนยันแล้ว (2026-08-14)** — ใช้เป็นฐานของ design ฉบับนี้

| # | ประเด็น | ข้อสรุป |
|---|---|---|
| 1 | ปี/วันที่ในไฟล์ | `Year` เป็น ค.ศ. อยู่แล้ว, `Inv Date` เป็น date serial ของ Excel, `Month` เป็น 1-12 |
| 2 | นำเข้าไฟล์ซ้ำ | **ทับด้วยค่าใหม่ (upsert)** ตามคีย์กันซ้ำ |
| 3 | ลูกค้าใหม่ | นับ **ใหม่ต่อบริษัท** + ผู้จัดการติ๊กลูกค้าเดิมไว้ก่อนได้ (`isPreExistingCustomer`) |
| 4 | คะแนนรวมเมื่อ KPI คำนวณไม่ได้ | **เฉลี่ยน้ำหนักใหม่เฉพาะเกณฑ์ที่คำนวณได้** + แสดง "คิดจาก X/5 เกณฑ์" |
| 5 | churn | ไม่สั่ง **6 เดือน** (แก้ได้ในหน้าตั้งค่า) |
| 6 | AI | **Gemini**, สร้างครั้งเดียวเก็บไว้ + ปุ่มสร้างใหม่, **เปิดการปิดบังชื่อ** ก่อนส่ง |
| 7 | Login | ใช้ **อีเมลเป็น identifier เฉย ๆ ไม่มีการส่งอีเมลจริง** — ไม่มีฟังก์ชัน "ลืมรหัสผ่าน" ทางอีเมล ผู้จัดการเป็นคนรีเซ็ตรหัสผ่านให้ ไม่เพิ่ม dependency บริการส่งอีเมล |
| 12 | sheet ในไฟล์ Excel | นำเข้า **sheet แรกเท่านั้น** sheet อื่นข้ามทั้งหมด |
| 13 | ลืมรหัสผ่าน | ผู้จัดการรีเซ็ตให้ + บังคับเปลี่ยนรหัสครั้งแรก (`mustChangePassword`) ไม่มีตาราง token และไม่มีบริการส่งอีเมล |
| 8 | Export | **Excel อย่างเดียว** |
| 9 | ยอดติดลบ/ใบลดหนี้ | **ไม่มี** — ไม่ทำ logic พิเศษ แต่ยังขึ้นเตือนถ้าเจอ |
| 10 | น้ำหนักคะแนน | **50 / 15 / 15 / 10 / 10** ตามที่เสนอ |
| 11 | `Year`/`Month` ไม่ตรงกับ `Inv Date` | **ยึด `Year`/`Month`** เป็นงวดบัญชี แล้วขึ้นเตือน |

**การตัดสินใจที่ผู้ใช้ยืนยันแล้ว (2026-08-16)** — รอบขยาย: แบ่งเครดิตดีล, ทะเบียนโรงพยาบาล/พื้นที่, ตัวช่วยตั้งเป้า · **ปิด blocker ครบทุกข้อแล้ว**

| # | ประเด็น | ข้อสรุปที่ผู้ใช้เลือก | สิ่งที่ถูกตัดออกด้วยการเลือกนี้ |
|---|---|---|---|
| 14 | ดีลที่แชร์เครดิตระหว่างพนักงานขาย | **เกิดขึ้นประจำ → รองรับใน schema** `SalesLineCredit` เป็นแหล่งเดียวของการรวมยอดรายคน | ตัดการรวมยอดผ่าน `SalesLine.salespersonId` ออกถาวร ห้ามมี fallback 2 ทาง |
| 15 | รวมชื่อโรงพยาบาลที่สะกดต่างกัน | normalize อัตโนมัติเมื่อชื่ออังกฤษตรง + เข้าคิวให้ผู้จัดการตัดสินเมื่อคลุมเครือ · ศิริราช กับ ศิริราช ปิยมหาราชการุณย์ = **คนละราย ห้ามรวม** | ตัดการรวมอัตโนมัติด้วยความคล้าย (fuzzy) ออก — ใช้การตรงกันแบบเป๊ะเท่านั้น |
| 16 | สูตรศักยภาพพื้นที่ | เตียง × น้ำหนักระดับ (เริ่มต้น 1.000) × ค่าปรับรายแห่ง **คำนวณแยกรายภาคเสมอ** + เกณฑ์ `minRegionCoverage` 0.50 | ตัดการคำนวณศักยภาพเป็นก้อนเดียวทั้งประเทศออก และตัด กทม. ออกจากการกระจายเป้าโดยอัตโนมัติ (registry ไม่มีกรุงเทพ) |
| 17 | **ลูกค้าใหม่ที่ปิดด้วยดีลแชร์เครดิต นับให้ใคร** (`requirement.md` ข้อ 18) | **ตามสัดส่วนเครดิตของบรรทัดแรกนั้น** — ดีล 50/50 = คนละ 0.5 ราย | ตัดทางเลือก "ทั้งคู่ได้เต็มคนละ 1 ราย" ออก · ได้กฎตรวจอัตโนมัติแลกมา: ผลรวมรายคน = จำนวนลูกค้าใหม่จริงของบริษัทพอดี |
| 18 | **พารามิเตอร์ตัวช่วยตั้งเป้า** (`requirement.md` ข้อ 10) | ย้อนหลัง **3 เดือน** · ดีลที่เกิน **40%** ของยอดคนนั้นในช่วงนั้น = ก้อนใหญ่ผิดปกติ ให้ตัดออกก่อนเฉลี่ย **พร้อมตีธงและเอากลับได้** · อัตราเติบโตเริ่มต้น 1.000 ผู้จัดการกรอกเอง | ตัดการฝังอัตราเติบโตที่ไม่มีใครยืนยันไว้ในระบบ · ตัดการตัด outlier แบบเงียบ ๆ ออก (ต้องแสดงรายการที่ตัดเสมอ) |
| 19 | **เป้าอ้างอิงรายภาคของสูตร `potentialBased`** | ใช้ **ผลรวมตัวเลขรายคนในภาคนั้น ไม่เพิ่มช่องกรอกเป้าบริษัท** · แก้การอ้างวนด้วยการนิยาม `R = Σ historyBased` ในโหมด SUGGEST และ `Σ Target` แบบ snapshot เฉพาะโหมด REBALANCE ที่ทุกคนมีเป้าครบแล้ว | ตัดฟิลด์/ตาราง "เป้าบริษัทรายปีรายภาค" ออกถาวร · ตัดการใช้ `Σ Target` สด ๆ เป็นตัวตั้งออก (อ้างวน — เหตุผลเต็มอยู่ใน Territory & Potential Rules ข้อ 5.2) |

**การตัดสินใจที่ผู้ใช้ยืนยันแล้ว (2026-08-16 รอบที่ 2)** — รอบรายงาน 3 ตัว + โครงสร้างเขต

| # | ประเด็น | ข้อสรุปที่ผู้ใช้เลือก | สิ่งที่ถูกตัดออกด้วยการเลือกนี้ |
|---|---|---|---|
| 20 | **ลำดับงาน** (`requirement.md` Scope ทักท้วงไว้) | ทำ **โครงสร้างเขต + เป้าระดับเขต ขนานไปกับรายงาน 3 ตัว** เร่งที่สุด · ไม่รอให้ `qa-engineer` ปิด Module J ก่อน · ส่วนทะเบียนสินค้าเริ่มได้ทันทีเพราะไม่ขึ้นกับเขต | ตัดทางเลือก "ปิดข้อ 0 ให้จบก่อน" ออก — **แลกมาด้วยความเสี่ยงที่รับไว้แล้ว**: ตัวเลขในรายงานอาจขยับเมื่อ Module J ถูกตรวจรับ ดู Risks ข้อ 26 |
| 21 | **เป้าของ 2 เขตที่ให้มาเป็นก้อนรวม** (Tasanee 14M = กท1+ภาคตะวันตก · Tanyapat 13M = กท2+ภาคกลาง) | **แตกเป็นตัวเลขรายเขต 1 แถวต่อ 1 เขต** ให้ตรงกับ grain ของ 10.7 | **ตัดโครงสร้าง "กลุ่มเขต" (target ผูกหลายเขต) ออกถาวร** ห้าม `backend-engineer` เพิ่มเอง · แลกมาด้วยงานข้อมูลฝั่งผู้ใช้: ต้องแตกตัวเลข 14M/13M ก่อนกรอก |
| 22 | **คะแนนรวม 0–100 ของเขต ตอนที่คำนวณได้แค่ 1 ใน 5 เกณฑ์** | **%ถึงเป้าเป็นตัวเลขหลัก · คะแนนรวมอยู่คนละคอลัมน์ พร้อมป้าย "คิดจาก X จาก 5 เกณฑ์" เสมอ** | ตัดทางเลือก "ซ่อนคะแนนรวมจนกว่าจะมี ≥ 2 เกณฑ์" ออก และตัดการแสดงคะแนนรวมโดยไม่มีป้ายกำกับออก |
| 23 | **เป้ารายคนหลังเป้าย้ายไปผูกเขต** | **derive อัตโนมัติ = เป้าของเขต ÷ จำนวนผู้ดูแล ACTIVE ของเขตนั้น** แล้วรวมทุกเขตที่คนนั้นดูแล | ตัดทางเลือก "ได้คนละเต็มจำนวน" และ "ไม่ derive เลย" ออก · **ชิงตัดสิน `requirement.md` OQ22 ไปก่อนโดยผู้ใช้รับทราบ** — ถ้า OQ22 ปิดด้วยกฎแบ่งแบบอื่นต้องกลับมาแก้ข้อนี้ ดู Territory KPI Rules ข้อ 6 |
| 24 | **รหัสสินค้าที่ยังว่างในระยะแรก** (`requirement.md` OQ11) | ทะเบียนสินค้าสร้างจากประวัติการขาย → `Product.code` ว่าง แสดง **"—" พร้อมคำเตือน** จนกว่าจะนำเข้าแคตตาล็อกจริงในระยะ 2 · รอบนี้ทำ `ProductAlias` (normalize + จำคำตัดสิน) | **เลื่อนคิวถามผู้จัดการเรื่องชื่อสินค้า (`ProductNameReview`) ไประยะ 2** — ระยะแรกไม่จำเป็นเพราะทะเบียนสร้างจากไฟล์เดียวกับที่ใช้จับคู่ จึงตรงกันเป๊ะโดยนิยาม |

**การตัดสินใจที่ผู้ใช้ยืนยันแล้ว (2026-08-19 รอบที่ 3)** — รอบสิทธิ์การมองเห็น + เป้าคู่เขต + การแทนที่งวด · **ยืนยันครบทุกข้อ ยกเว้นแถวที่ 30 ที่ยังไม่ได้ถาม**

| # | ประเด็น | ข้อสรุปที่ผู้ใช้เลือก | สิ่งที่ถูกตัดออกด้วยการเลือกนี้ |
|---|---|---|---|
| 25 | **Leaderboard ชั้นหลัก (ทุกคนเห็นทุกเขต) ขัดกับ 10.7** (พนักงานขายทั่วไป "ไม่เห็นเขตอื่นแม้แต่แถวสรุป") — ทั้งสองข้อผู้ใช้ยืนยันวันเดียวกัน | **ทางสายกลาง: เห็น "อันดับ + คะแนนรวม 0–100" ของทุกเขต แต่ไม่เห็นตัวเงิน · ไม่เห็น %ถึงเป้า · ไม่เห็นคะแนนแยกราย KPI** · ใช้กฎเดียวกันทั้ง Leaderboard ชั้นหลักและรายงาน 10.7 · **เขตของตัวเองเห็นเต็ม** · **MANAGER และ supervisor ของเขตนั้นไม่เปลี่ยน เห็นเต็มเหมือนเดิม** | ตัดทางเลือก "ตัวเลขระดับเขตเป็นสาธารณะทั้งหมด" และ "พนักงานขายไม่เห็นแถวของเขตอื่นเลย" ออกทั้งคู่ · **แลกมาด้วยกฎที่ต้องระบุเป็นรายฟิลด์** — นิยาม whitelist อยู่ใน Data Visibility Rules ข้อ 6 · **ตาราง 10.7 ใน `requirement.md` ต้องให้ `business-analyst` แก้ตาม** (เดิมเขียนว่าไม่เห็นแถวนั้นเลย) |
| 26 | **เป้าคู่เขต 14M/13M** (`requirement.md` OQ26) | **(ก) `TerritoryGroup` + `TerritoryGroupMember`** — 1 เขตอยู่ได้ไม่เกิน 1 กลุ่ม · เป้าผูกที่กลุ่ม · **grain "1 แถว = 1 เขต" ของ 10.7 ไม่เปลี่ยน** · หน่วยจัดอันดับ = "หน่วยเป้า" | **กลับการตัดสินใจแถวที่ 21** (ที่เคยตัด "กลุ่มเขต" ออกถาวรและให้ผู้ใช้แตกตัวเลขเอง) — โดยผู้ใช้ยืนยันเอง · ตัดทางเลือก self-relation คู่เขต, การแตกตัวเลขเอง, และการผูกเป้าไว้ที่ `TerritoryAssignment` ออกทั้งหมด |
| 27 | **ผลข้างเคียง: 8 ใน 10 คนดูแลเขตของตัวเองคนเดียว** ตัวเลขของเขตจึงเท่ากับตัวเลขของคนนั้น | **ผู้ใช้รับทราบและยอมรับแล้ว** — เพราะเมื่อรวมกับการตัดสินใจแถวที่ 25 สิ่งที่เปิดเผยคือ **"อันดับเชิงเปรียบเทียบ" ไม่ใช่ข้อมูลธุรกิจจริง** (ไม่มีตัวเงิน ไม่มี %ถึงเป้า) · และ **บล็อก `personalBucket` / `unassignedBucket` แสดงเฉพาะ MANAGER** | ตัดความคาดหวังที่ว่านโยบาย "เห็นเฉพาะข้อมูลของตัวเอง" จะปิดการเปรียบเทียบระหว่างบุคคลได้ 100% — เปิดเผยอันดับโดยตั้งใจ ดู Risks ข้อ 28 ฉบับแก้ |
| 28 | **supervisor สั่งสร้าง AI insight ให้ตัวเองได้หรือไม่** | **ได้** — เขาเป็นสมาชิกของทีมในเขตที่ตัวเองเป็น supervisor จริง | ตัดกรณียกเว้น "ทุกคนในเขตยกเว้นตัวเอง" ออก — ทำให้กฎเหลือประโยคเดียวที่ engineer ไม่ต้องตีความ |
| 29 | **บัญชี `SALESPERSON` ที่ยังไม่ได้ผูกกับ `Salesperson`** | ข้อมูลของคนอื่น → **`403`** · หน้าของตัวเอง → `200` พร้อม payload ว่างและ `reason = "ACCOUNT_NOT_LINKED"` · หน้าจัดการผู้ใช้ต้องขึ้นเตือนบัญชีที่ยังไม่ผูก | ตัดพฤติกรรมเดิมที่บัญชีแบบนี้เห็นข้อมูลทุกคน (Phase 7 เคยยืนยันว่าได้ `200`) ออกถาวร |
| 30 | **กลไกแทนที่/ลบข้อมูลตามงวด** (Module R) | **archive แล้วลบจริง** (`SalesLineArchive`) · **dry-run บังคับ** ก่อนยืนยันเสมอ · **กู้คืนด้วยสคริปต์ของทีมพัฒนา ไม่มีปุ่มให้ผู้จัดการกดเอง** · **ขอบเขตที่ผู้ใช้ยืนยันชัด: ใช้เฉพาะกรณี "นำเข้าไฟล์ของงวดเดิมซ้ำเพื่อแก้ข้อมูลที่ผิด" เท่านั้น — งวดใหม่ยังสะสมข้อมูลเก่าไว้ตามปกติ ไม่เกี่ยวกับกลไกนี้** · ยอมรับข้อจำกัดชั่วคราวว่า **ห้ามนำเข้าไฟล์ของงวดเดิมซ้ำจนกว่า Module R จะลง** | ตัด soft-delete (`supersededAt` บน `SalesLine`) ออกถาวร · ตัดปุ่มกู้คืนบนหน้าจอออกจากรอบนี้ · ตัดการให้ระบบเดาโหมดเองจากการที่งวดนั้นมีข้อมูลอยู่แล้วออก (ผู้จัดการต้องเลือกโหมดเสมอ) |
| 31 | **เขตเข้า/ออกกลุ่มเป้าในอนาคต** | `TerritoryGroupMember` ต้องมี `effectiveFrom`/`effectiveTo` เพื่อให้เขตเป็นสมาชิกเฉพาะช่วงนั้น; หนึ่งเขตอยู่ได้ไม่เกินหนึ่งกลุ่ม **ในช่วงเวลาที่ทับกัน** · เป้ารายเขตและสมาชิกกลุ่มห้ามทับกันในเดือนเดียว | ตัดแบบเดิมที่เป็นสมาชิกตลอดไปและการลบแถวสมาชิกทิ้งออก — ห้ามมีทั้ง `Target(scope=TERRITORY)` และสมาชิกกลุ่มที่มีผลในงวดเดียวกัน แล้วเลือกใช้ใน query ภายหลัง |

**การตัดสินใจที่ผู้ใช้ยืนยันแล้ว (2026-08-22 รอบที่ 5)** — การ seed จังหวัดใน Module K / Phase 9

| # | ประเด็น | ข้อสรุปที่ผู้ใช้เลือก | สิ่งที่ถูกตัดออกด้วยการเลือกนี้ |
|---|---|---|---|
| 32 | **การจัด 4 จังหวัดที่บางนิยามนับเป็นภาคกลาง** — นครสวรรค์, กำแพงเพชร, พิจิตร, อุทัยธานี | ใช้การแบ่งภูมิภาคแบบ 5 ภาคปกติของระบบนี้: ทั้ง 4 จังหวัดอยู่ **ภาคเหนือ** | ตัดการจัด 4 จังหวัดนี้เป็นภาคกลาง และตัดการรอถามซ้ำก่อน seed ออก |

---

## Unresolved Open Questions — closed items (archived 2026-08-25)

ข้อที่ปิดแล้วจากหัวข้อ `## Unresolved Open Questions` ใน `design.md` (17 จาก 22 ข้อ ณ วันที่เก็บ archive) — เก็บคำต่อคำรวมบล็อก "บริบทเดิม (เก็บไว้เป็นประวัติ)" ของข้อ 16 และ 17 ที่ตัวเอกสารเองระบุไว้แล้วว่าเป็นประวัติ ข้อที่ยังเปิดอยู่จริง (8, 10, 14, 20) ไม่ได้ย้ายมาที่นี่ — ยังอยู่ใน `design.md` ตามเดิม

1. ✅ **ปิดแล้ว 2026-08-16 — ลูกค้าใหม่ที่ปิดได้ด้วยดีลแชร์เครดิต นับให้ใคร** (`requirement.md` Open Question ข้อ 18) ผู้ใช้เลือก **(ค) ตามสัดส่วนเครดิตของบรรทัดแรกนั้น** — ดีล 50/50 นับให้คนละ 0.5 ราย ผลรวมรายคนจึงเท่ากับจำนวนลูกค้าใหม่จริงของบริษัทพอดีและใช้เป็นกฎตรวจอัตโนมัติได้ · นิยามที่ผูกพันอยู่ในหัวข้อ KPI & Scoring Rules แถว `NEW_CUSTOMERS` · **ไม่แตะน้ำหนัก 15% และไม่แตะสูตรคะแนนรวมของ Phase 4** แต่ตัวเลข `NEW_CUSTOMERS` ของดีลที่แชร์กันจะเปลี่ยน จึงอยู่ในขอบเขตที่ `qa-engineer` ต้องตรวจ Phase 4–7 ซ้ำอยู่แล้ว
2. ✅ **ปิดแล้ว 2026-08-16 — พารามิเตอร์ของตัวช่วยตั้งเป้า** (`requirement.md` Open Question ข้อ 10) ย้อนหลัง **3 เดือน** (`targetLookbackMonths`) · เกณฑ์ดีลก้อนใหญ่ผิดปกติ **40%** ของยอดคนนั้นในช่วงย้อนหลัง (`targetOutlierThreshold`) โดยหน่วยของดีลคือ 1 `invoiceNo` และต้อง **แสดงรายการที่ตัดพร้อมปุ่มเอากลับ ห้ามตัดเงียบ** · อัตราเติบโตเริ่มต้น **1.000** (`targetGrowthRate`) ผู้จัดการกรอกเองต่อรอบ · สูตรเต็มอยู่ใน Territory & Potential Rules ข้อ 5.1
3. ✅ **ปิดแล้ว 2026-08-22 — ตอบโดย `requirement.md` OQ18 (ปิด 2026-08-18) + Phase 8 ที่ build ตามนั้น**: ค่าเริ่มต้น = แบ่งเท่ากันอัตโนมัติ (`SHARED_CREDIT_RULE_CREATED`) และผู้จัดการแก้สัดส่วนที่**ระดับกฎ** (`SalesmanNameRule`) ผ่านหน้าคิวยืนยัน — ไม่ใช่ 50/50 ถาวรที่ตัด UI ทิ้ง และไม่ใช่การแก้รายดีล · ข้อนี้จึงไม่มีสิ่งใดต้องตัดสินเพิ่ม
4. ✅ **ปิดแล้ว 2026-08-22 — การจัด 4 จังหวัดเข้าภาค** — นครสวรรค์ กำแพงเพชร พิจิตร อุทัยธานี อยู่ **ภาคเหนือ** ตามการแบ่งภูมิภาคแบบ 5 ภาคปกติของระบบนี้ · Phase 9 seed ต้องสร้าง `ProvinceMapping` ของทั้ง 4 จังหวัดโดยอ้าง `Region` ภาคเหนือ (การตัดสินใจแถวที่ 32)
5. ✅ **ปิดแล้ว 2026-08-16 — เป้าบริษัทรายภาคมาจากไหน** ผู้ใช้เลือก "ใช้ผลรวมตัวเลขรายคนในภาคนั้น **ไม่เพิ่มช่องกรอกเป้าบริษัท**" · การใช้ผลรวม `Target` ที่บันทึกแล้วตรง ๆ **อ้างวน** (ยังไม่มีเป้า→R=0 · ผลขึ้นกับลำดับการบันทึก · รับข้อเสนอแล้วข้อเสนอเปลี่ยนตัวเอง) จึงนิยามใหม่เป็น `R = Σ historyBased` ของภาคนั้นในโหมด SUGGEST ซึ่งไม่ขึ้นกับเป้าที่มีอยู่ และเก็บความหมายตามตัวอักษร (`Σ Target` แบบ snapshot) ไว้เฉพาะโหมด REBALANCE ที่ทุกคนในภาคมีเป้าครบแล้ว · เหตุผลเต็มและคุณสมบัติ `Σ potentialBased = Σ historyBased` อยู่ใน Territory & Potential Rules ข้อ 5.2 — **สอดคล้องกับ `requirement.md` ที่ระบุว่าไม่มีเป้าระดับทีม เพราะไม่มีตัวเลขใหม่ที่ใครต้องกรอกเพิ่มเลย**
6. ✅ **ปิดแล้ว 2026-08-16 (รอบที่ 2) — แหล่งที่มาของรหัสสินค้า** (`requirement.md` Open Question ข้อ 11) รหัสมาจาก **ทะเบียนสินค้าในระบบ** ไม่ใช่คอลัมน์ในไฟล์ Excel · ระยะแรกทะเบียนสร้างจากประวัติการขาย ทำให้ `Product.code` เป็น `null` ทั้งหมดและต้องแสดง "—" พร้อมคำเตือน · ดู Product Master & Ranking Rules · **คำถามที่แตกออกมาแทนและยังค้าง**: ผู้ใช้จะส่งรายการสินค้าทั้งหมดของบริษัทมาเมื่อไหร่ — จนกว่าจะได้ไฟล์นั้น รายการ "สินค้าที่ขายไม่ได้เลย" ยังเป็นแค่ช่องว่างเชิงเปรียบเทียบระหว่างเขต
7. ✅ **ปิดแล้ว 2026-08-19 — คำสั่ง "ลบข้อมูลตามงวด" จำเป็นจริง และออกแบบเสร็จแล้ว** (`requirement.md` OQ8(ก) ยืนยันว่า 1 ไฟล์ = 1 งวดทั้งงวด) · ออกแบบเป็น **Module R** ดูหัวข้อสัญญา **Period Replace & Delete Rules** · **แก้ข้อความเดิมของข้อนี้ที่ว่า "ไม่ต้องแก้ schema" — ผิด**: ต้องเพิ่ม `SalesLineArchive` + `ImportMode`/`ArchiveReason` + 4 คอลัมน์บน `ImportBatch` (ทั้งหมดเพิ่มล้วน) เพราะการลบแบบไม่เก็บสำเนาคือการทำลายข้อมูลที่ผู้ใช้เคยเห็นตัวเลขไปแล้วโดยกู้คืนไม่ได้ · **✅ ผู้ใช้ยืนยันรูปแบบครบแล้ว 2026-08-19** (การตัดสินใจแถวที่ 30)
9. ✅ **ปิดแล้ว 2026-08-22 — คำตอบอยู่ที่ `requirement.md` OQ8 ที่ปิดไปแล้ว**: ad-hoc แต่หลัก ๆ เป็นรอบเดือน (1 ไฟล์ = 1 งวดทั้งงวด) — ไม่กระทบ design มีผลแค่ความสดของ Dashboard
11. ✅ **ล้าสมัยแล้ว 2026-08-22 — ถูกแทนที่ด้วยข้อ 16**: ผู้ใช้เลือก `TerritoryGroup` + `TerritoryGroupMember` (2026-08-19, การตัดสินใจแถวที่ 26) ทำให้ **ไม่ต้องแตกตัวเลขเป้า 14M/13M เป็นรายเขตอีกต่อไป** — เป้ารวมกรอกที่ระดับกลุ่มได้ทันทีที่ Module M ลง (บริบทเดิม: ผู้ใช้เคยเลือกแตกเป็นรายเขตตามการตัดสินใจแถวที่ 21 แต่ยังไม่ได้ให้ตัวเลขที่แตกแล้ว — กท1 / ภาคตะวันตก · กท2 / ภาคกลาง — เป็นงานข้อมูล ไม่ใช่งานโค้ด)
12. ✅ **ปิดแล้ว 2026-08-22 — ผู้ใช้เลือก "ระบบเปิดให้กรอกเองได้"** (คู่กับ `requirement.md` OQ20 ที่ปิดวันเดียวกัน): ไม่มีวันที่จริงให้วันนี้และห้าม agent เดา แต่ **ระบบต้องเปิดให้ผู้จัดการกรอก `Salesperson.employmentEndedAt` และปิด `TerritoryAssignment.effectiveTo` ผ่านหน้าจอเองเมื่อทราบวันที่จริง** · ⚠️ ตรวจโค้ด 2026-08-22 แล้ว: **ยังไม่มี endpoint/UI ใดรับค่าทั้งสองฟิลด์นี้** (`PUT /territory-assignments` ของ Phase 12 รับ `effectiveTo` ฝั่ง assignment แล้ว แต่ `employmentEndedAt` ไม่มีช่องทางเขียนเลย) → เป็นงาน implement ใหม่ที่ต้องให้ `project-manager` จัดเฟส · จนกว่าจะกรอก: ค่ายังเป็น `null` และ `activeOwnerCount` ของงวดย้อนหลังยังคลาดเคลื่อนตามที่ผู้ใช้ยอมรับไว้
13. ✅ **ปิดแล้ว 2026-08-22 — `requirement.md` OQ22 ยืนยันกฎเดียวกันนี้แล้ว (2026-08-18)**: "หารเท่ากันตามจำนวนผู้ดูแล ACTIVE" ไม่ใช่การชิงตัดสินอีกต่อไป จึงไม่มีอะไรต้องกลับมาแก้ Territory KPI Rules ข้อ 6
15. ~~**สิทธิ์การมองเห็นรายงานรายเขต** — รอบนี้ยึดกฎเดิมของทั้งระบบ (ทุกคนที่ล็อกอินเห็นทุกอย่าง ต่างกันที่สิทธิ์แก้ไข)~~ — **ล้าสมัยแล้ว 2026-08-19** ผู้ใช้กลับนโยบายและปิด `requirement.md` OQ25 ครบแล้ว: บังคับ 3 ชั้นทั้งระบบ ดูหัวข้อสัญญา **Data Visibility Rules** และ Module Q · ส่วนที่ยังเปิดคือความขัดกันเรื่องตัวเลขระดับเขต → ย้ายไปเป็นข้อ 17
16. ✅ **ปิดแล้ว 2026-08-19 — ผู้ใช้เลือก (ก) `TerritoryGroup` + `TerritoryGroupMember`** (การตัดสินใจแถวที่ 26) · ทางเลือกที่ถูกตัดออกและเหตุผลเก็บไว้ในตารางด้านล่างเป็นประวัติ · **เป้า 14M (กท1+ภาคตะวันตก) และ 13M (กท2+ภาคกลาง) กรอกเข้าระบบได้ทันทีที่ Module M ลง โดยไม่ต้องให้ผู้ใช้แตกตัวเลข** · สัญญาเต็มอยู่ใน Territory KPI Rules ข้อ 6

    **บริบทเดิม (เก็บไว้เป็นประวัติ) — เป้าของ 2 เขตที่คนเดียวดูแล จะเก็บและวัดอย่างไร** (`requirement.md` OQ26 — Tasanee 14M รวม กท1+ภาคตะวันตก · Tanyapat 13M รวม กท2+ภาคกลาง) · ผู้ใช้ระบุชัดว่า **"แต่ละเขตไม่กำหนดเป้าแยก แต่ผลรวมของสองเขตนั้นต้องได้ตามเป้ารวม"** ซึ่งขัดกับการตัดสินใจแถวที่ 21 ที่ตัด "กลุ่มเขต" ออกถาวรเมื่อ 2026-08-16 · **ต้องเลือก 1 ใน 4 ทาง**

    | ทางเลือก | วิธี | ได้อะไร | เสียอะไร |
    |---|---|---|---|
    | **(ก) `TerritoryGroup` — เอกสารนี้แนะนำ** | เพิ่ม 2 ตาราง + `Target.territoryGroupId` + `TargetScope.TERRITORY_GROUP` · 1 เขตอยู่ได้ไม่เกิน 1 กลุ่ม | เก็บเป้าได้ตรงตามที่ผู้ใช้พูดเป๊ะ · **grain "1 แถว = 1 เขต" ของ 10.7 ไม่เปลี่ยนเลย** · สมการยอดรวม 3 ก้อนไม่เปลี่ยนเลย · 4 เขตนั้นยังเข้า Leaderboard ได้ในฐานะสมาชิกของหน่วยเป้า | +2 ตาราง +1 คอลัมน์ · กลับการตัดสินใจแถวที่ 21 · รายงาน/Leaderboard ต้องมีแถวระดับกลุ่มเพิ่มอีกชนิดหนึ่ง |
    | **(ข) self-relation คู่เขตบน `Territory`** | `Territory.combinedTargetWithId String? @unique` เขตหนึ่งถือเป้า อีกเขตชี้มาหา | เพิ่มแค่ 1 คอลัมน์ ไม่มีตารางใหม่ | รองรับได้แค่ "คู่" ถ้าอนาคตเป็น 3 เขตต้องรื้อ · ความสัมพันธ์ไม่สมมาตร อ่านโค้ดแล้วงงว่าเป้าอยู่ฝั่งไหน · ต้องมี validator กัน chain (A→B→C) เอง |
    | **(ค) แตกตัวเลขเอง** (= การตัดสินใจแถวที่ 21 เดิม) | ผู้ใช้ระบุตัวเลขรายเขต เช่น กท1 8M / ภาคตะวันตก 6M | **ไม่ต้องแก้ schema เลย ถูกที่สุด** และทุกอย่างที่ออกแบบไว้แล้วใช้ได้ทันที | ผู้ใช้ปฏิเสธไปแล้วรอบหนึ่ง · ตัวเลขที่แตกจะกลายเป็นเกณฑ์วัดจริงของแต่ละเขต ทั้งที่ผู้ใช้ตั้งใจวัดรวม |
    | **(ง) ผูกเป้าไว้ที่ `TerritoryAssignment` ของคนคนนั้น** | เป้าติดที่คู่ (คน × เขต) | ตรงกับความรู้สึกว่า "เป็นเป้าของ Tasanee" | **ไม่แนะนำ** — ขัดกับ `requirement.md` 10.6 ที่ยืนยันว่า "เป้าเป็นของเขต" · เขตที่ไม่มีผู้ดูแลจะไม่มีเป้าทันที · เป้าย้อนหลังหายเมื่อผู้ดูแลเปลี่ยน · %ถึงเป้าของเขตจะขึ้นกับว่าใครถูก assign อยู่ ซึ่งเป็นสิ่งที่ทั้งเอกสารนี้ออกแบบมาเพื่อหลีกเลี่ยง |

    ~~**ผลถ้ายังไม่ตอบ**: Module M implement ส่วนอื่นไปได้ แต่ **กรอกเป้า 14M/13M เข้าระบบไม่ได้** และ 4 เขตนั้นจะแสดง "ยังไม่ได้ตั้งเป้า" ทำให้ **จัดอันดับใน Leaderboard ชั้นหลักไม่ได้**~~ — **ไม่เป็นปัญหาอีกต่อไป ผู้ใช้ตอบแล้ว**

17. ✅ **ปิดแล้ว 2026-08-19 — ผู้ใช้เลือกทางสายกลางที่ไม่อยู่ใน 3 ทางเลือกเดิม: แบ่งตาม *ฟิลด์* ไม่ใช่ตาม *แถว*** (การตัดสินใจแถวที่ 25) · **เห็นได้ทุกเขต: อันดับ + คะแนนรวม 0–100 · เห็นไม่ได้: ตัวเงิน · %ถึงเป้า · คะแนนแยกราย KPI** · เขตของตัวเองเห็นเต็ม · MANAGER/supervisor ของเขตนั้นไม่เปลี่ยน · ใช้กฎเดียวกันทั้ง Leaderboard ชั้นหลักและรายงาน 10.7 · whitelist รายฟิลด์อยู่ใน **Data Visibility Rules ข้อ 6** · ⚠️ **`business-analyst` ต้องแก้ตาราง 10.7 ใน `requirement.md` ตาม** (เดิมเขียนว่าไม่เห็นแถวของเขตอื่นเลย)

    **บริบทเดิม (เก็บไว้เป็นประวัติ) — ตัวเลขระดับเขต: สาธารณะ หรือเห็นเฉพาะเขตของตัวเอง** — `requirement.md` ยืนยัน 2 ข้อที่ขัดกันในวันเดียวกัน (OQ29/Core Features ข้อ 6 = "ทุก role เห็นเหมือนกันหมด ไม่กรอง" · OQ25(ค)/10.7 = "ไม่เห็นเขตอื่นแม้แต่แถวสรุป") · **ต้องเลือก 1 ใน 3 ทาง**

    | ทางเลือก | กฎที่ได้ | ผล |
    |---|---|---|
    | **(ก) ตัวเลขระดับเขตเป็นสาธารณะทุกหน้าจอ — เอกสารนี้แนะนำ** | "aggregate ระดับเขต = ทุกคนเห็น · ข้อมูลรายคน = จำกัด 3 ชั้น" | กฎเดียว ไม่มีจุดให้ engineer ตัดสินซ้ำในแต่ละหน้าจอ · ตรงกับเหตุผลที่ผู้ใช้ให้ไว้ใน OQ29 เอง · **ต้องให้ `business-analyst` แก้ตาราง 10.7 ใน `requirement.md` ตาม** · ยอมรับ Risks ข้อ 28 |
    | **(ข) ทั้งสองอย่างตามตัวอักษร** | Leaderboard เห็นทุกเขต แต่รายงาน 10.7 เห็นเฉพาะเขตตัวเอง | เอกสารขัดกันเองต่อไป — คนเดียวกันเห็นยอดเขต ก. ในหน้าหนึ่งแต่ไม่เห็นในอีกหน้าหนึ่ง · **ไม่แนะนำ** เพราะการกั้นข้อมูลที่รั่วอยู่แล้วอีกหน้าหนึ่งไม่ได้กั้นอะไรจริง |
    | **(ค) ชั้นหลักเห็นได้แต่ไม่มีตัวเงิน** | Leaderboard แสดง **อันดับ + คะแนนรวม + %ถึงเป้า** ของทุกเขต แต่ **ยอดเงิน/รายชื่อโรงพยาบาล/drill-down** เห็นเฉพาะเขตของตัวเอง (10.7 คงกฎเดิม) | รักษาเจตนาของทั้งสองข้อได้มากที่สุด · แลกกับกฎที่ซับซ้อนขึ้น 1 ชั้น (ต้องแยก "ฟิลด์สาธารณะ" กับ "ฟิลด์จำกัด" ในตัวเลขชุดเดียวกัน) — ถ้าเลือกทางนี้ **ต้องระบุรายฟิลด์ว่าอันไหนสาธารณะ** ให้ครบก่อน implement |

    ~~**ระหว่างที่ยังไม่ตอบ**: implement แบบ **fail closed**~~ — **ไม่ต้องใช้แล้ว มีคำตอบจริงแทน**

18. ✅ **ปิดแล้ว 2026-08-19 — `personalBucket` / `unassignedBucket` แสดงเฉพาะ MANAGER** (การตัดสินใจแถวที่ 27) · ห้ามแสดงแม้ในรูปตัวเลขรวมที่ไม่มีชื่อ เพราะวันนี้ `personalBucket` = ยอดของ Mr.Sathit พอดี
19. ✅ **ปิดแล้ว 2026-08-19 — supervisor สั่งสร้าง AI insight ให้ตัวเองได้** (การตัดสินใจแถวที่ 28) เพราะเขาเป็นสมาชิกของทีมในเขตนั้นจริง · กฎอยู่ใน Data Visibility Rules ข้อ 3
21. ✅ **ปิดแล้ว 2026-08-19 — รูปแบบของกลไกแทนที่/ลบข้อมูลตามงวด (Module R)** ผู้ใช้ยืนยันครบทั้ง 3 ข้อย่อย (การตัดสินใจแถวที่ 30): **(ก)** archive แล้วลบจริง แทน soft-delete · พร้อม**ยืนยันขอบเขตด้วยคำของตัวเองว่ากลไกนี้ใช้เฉพาะกรณีนำเข้าไฟล์ของงวดเดิมซ้ำเพื่อแก้ข้อมูลผิด ส่วนงวดใหม่ยังสะสมข้อมูลเก่าตามปกติ** · **(ข)** กู้คืนด้วยสคริปต์ของทีมพัฒนา ไม่มีปุ่มให้ผู้จัดการกดเองในรอบนี้ · **(ค)** ยอมรับข้อจำกัดชั่วคราวว่า **ห้ามนำเข้าไฟล์ของงวดเดิมซ้ำจนกว่า Module R จะลง** (Risks ข้อ 30) · **schema ของ Module R พร้อม propagate แล้ว**
22. ✅ **ออกแบบแล้ว 2026-08-22 — ⏳ รอผู้ใช้ยืนยัน schema ก่อน implement** — นโยบายจากผู้ใช้ (`requirement.md` OQ30): ถ้าชื่อในไฟล์อาจซ้ำ ให้ user เลือกเอง · **แบบที่ออกแบบ**: model `SalesmanNameReview` (Data Model, reuse enum `NameReviewStatus`) + Import Rules กฎชื่อพนักงานขายข้อ 8 — import ยัง auto-create ตามเดิม (**ไม่บล็อกไฟล์**) แต่เปิดคิวให้ผู้จัดการเลือก KEPT_SEPARATE ("คนใหม่จริง") / MERGED ("ซ้ำ → ย้ายทุก reference ไปคนเป้าหมาย ลบแถวซ้ำ ไม่ถามซ้ำ") · เคสจริง `Miss Napatsorn  Dadphu` (ไม่มีคู่ซ้ำใน DB) จะเข้าคิวนี้ให้ผู้จัดการกดยืนยันเป็นคนใหม่เอง

---

## Change Log

- 2026-08-25 — สร้างไฟล์นี้ครั้งแรกจาก one-time catch-up archiving round (`policies/documentation.md` §4 "Catching up a document that grew bloated before it was ever archived" — `design.md` ไม่เคยผ่านการ archive มาก่อนเลยตั้งแต่สร้าง ทำโดย session ที่กำลังทำงานในบทบาท `project-manager`/`pm` ตามที่ผู้ใช้สั่ง "ทำเลย" ต่อจากที่เสนอไว้ ไม่ใช่ `system-analyst` แต่ policy อนุญาตให้ "whoever notices" ทำ catch-up ได้): ย้ายตารางการตัดสินใจ 5 ชุด (แถวที่ 1–32) จาก `## Feature-by-Feature Feasibility` และรายการที่ปิดแล้ว 17 ข้อ (รวม 2 บล็อก "บริบทเดิม" ของข้อ 16/17) จาก `## Unresolved Open Questions` มาไว้ที่นี่แบบคำต่อคำ — ไม่มีเนื้อหาใดถูกสรุปหรือลบทิ้ง ดู `design.md`'s Change Log entry วันเดียวกันสำหรับ pointer ที่ทิ้งไว้แทนที่
