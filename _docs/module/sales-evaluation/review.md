# ระบบประเมินและสนับสนุนพนักงานขาย (Sales Evaluation) — Verification & Review

## Open Issues — all phases

| Issue | Phase | Routes to | Blocking |
|---|---|---|---|
| ~~`skippedRows` on `ImportBatch` needs its definition clarified~~ **Definition closed** in `design.md` (2026-08-16: blank/separator rows count as `skippedRows`, equation `totalRows = inserted + updated + skipped + error`), but the main import path still hardcodes `skippedRows: 0` and never writes the `BLANK_ROWS_SKIPPED` aggregate issue — implementation gap only | Phase 2 (Module C) → `review/phase-2.md` | `backend-engineer` | No — cosmetic/informational, not a data-correctness issue |
| ~~Security review outstanding — Module C~~ **✅ ปิดแล้ว 2026-08-22** — `security` ผ่าน (`security.md`): upload caps/filters/advisory-lock/parameterized queries verified; F-C1 workbook-size guard (Low 🔵) เปิดค้างเป็น hardening suggestion | Phase 2 (Module C) → `security.md` | closed (findings → `backend-engineer`) | No |
| ~~Security review outstanding — Module B (auth, passwords, JWT, personal data)~~ **✅ ปิดแล้ว 2026-08-22** — `security` ผ่าน (`security.md`): bcrypt + per-request DB check (deactivation = instant revocation) + JWT env secret/expiry + mustChangePassword gate; F-B1 login rate limiting (Medium 🔵) เปิดค้าง — แนะนำแก้ก่อน expose สาธารณะ | Phase 1 (Module B) → `security.md` | closed (findings → `backend-engineer`) | No |
| ~~Security review outstanding — Module G (AI Coaching)~~ **✅ ปิดแล้ว 2026-08-22** — `security` ผ่าน (`security.md`): key server-side only (frontend grep clean), timeout+fallback, anonymization applied at call site, snapshot auditable; F-G1 key-in-URL (Low 🔵) + F-G2 sentToGemini semantics (Low 🔵) เปิดค้าง | Phase 6 (Module G) → `security.md` | closed (findings → `backend-engineer`) | No |
| Phase 2 and Phase 3's most recent verification rounds were **TARGETED**, not FULL. Per this file's own gate ("Deploy eligibility requires a FULL round"), `devops` cannot treat either phase as deploy-eligible on a TARGETED round alone, regardless of how clean the targeted fix-checks came back | Phase 2/3 (Modules C/D) → `review/phase-2.md` / `review/phase-3.md` | `qa-engineer` (run one FULL round on each before deploy) | **Yes** — for `devops` eligibility specifically; does not call the phases' underlying correctness into question |
| **Phase 6's most recent verification round is now TARGETED** (this round, 2026-08-16 — see below), superseding its previous FULL round (Phase 5/6, 2026-08-15). Per this file's own gate, `devops` cannot treat Phase 6 as deploy-eligible until one more FULL round runs, even though this TARGETED round confirmed the SALESPERSON→403 fix is correct and live-tested and closed two previously-open items outright | Phase 6 (Module G) → this round, below | `qa-engineer` (run one FULL round before deploy) | **Yes** — for `devops` eligibility specifically; does not call the fix's correctness into question |
| `qa.temp.user@example.com` leftover test account still present in the dev DB (`isActive: false`) | Phase 1 (Module B) → `review/phase-1.md` | — (cleanup only, no specific owner) | No |
| **Phase 2/3/6 FULL re-rounds still owed** — their most recent rounds are TARGETED and shared surfaces have since drifted (`import.service.ts` gained Module J rewrite + product alias + salesman-review hook + REPLACE_PERIOD; target/kpi/coaching controllers touched by later phases' work). Prior task-level verification does not automatically carry to the current tree | Phase 2/3/6 (Modules C/D/G) | `qa-engineer` (one FULL round each over the current code before any deploy claim) | **Yes** — for `devops` eligibility specifically |
| Security review — Module D (Targets) isn't in `design.md`'s explicit `⚠️ Sensitive` list, but its write paths carry financial figures (`revenueTarget`); non-blocking completeness suggestion, not a required gate | Phase 3 (Module D) → `review/phase-3.md` | `security` | No — suggestion only |
| **Real-data territory setup is entirely pending** — 0 `Territory`/`TerritoryAssignment`/`TerritoryGroup` rows and no `Salesperson.excludedFromTerritoryTotals = true` flag exists (plan Phase 12's migration task says to set it for Mr.Sathit via data): today every real-period revenue lands in `unassignedBucket` (14,555,965.12), `personalBucket` reads 0, and all Module N/18 screens show empty territory tables. This is the already-recorded "manager creates territories/assignments through UI → run bootstrap" data work, NOT a code defect; re-verify Module N numbers once real setup lands | Phase 12/13 (Modules M+N) → this row + `status.md` Now line | user + `backend-engineer` (data work) | No — fixtures proved the code correct; blocks only real-data usefulness |
| **Module N (Phase 13) now carries `🔒 Security gate`** — added 2026-08-24 by `qa-engineer`: it implements field-level visibility enforcement (whitelist stripping serializer, bucket gating, drill-down 403s, export parity) where one missed branch silently leaks business figures. Required `security` pass before any deploy claim, same class as Modules Q/R | Phase 13 (Module N) → this round below / `security.md` when run | `security` | **Yes** — for `devops` eligibility of Phase 13 |
| Responsive layout at actual mobile viewport widths has never been visually verified across **any** phase that has UI — Phase 5/6 (dashboard, leaderboard, coaching panel) and Phase 7 (individual report, team-overview report) all show sound mobile-first Tailwind patterns on inspection (`grid-cols-1`/`lg:grid-cols-2`, `flex-wrap`, `p-4 sm:p-6`, `max-w-5xl`/`max-w-3xl`), but no round has had browser/viewport automation tooling available to actually render and check. Phase 13's `/territory-kpi` page joins this list (sound patterns on inspection, unverified visually); Phase 12's seven pages (territories manager, assignments manager/history, hospital-moves, unassigned-hospitals, territory-targets grid, TerritoryGroupManager, my-territory) join the same list 2026-08-24 — sound-on-inspection, unverified-visually | Phase 5 (Module F), carried through Phase 7 (Module H) and Phase 13 (Module N) → `review/phase-5-6.md`, `review/phase-7.md` | `qa-engineer` (re-run with browser/viewport tooling in a future round) | No — non-blocking; the one `plan.md` checkbox for this (Phase 5's) is left unchecked pending an actual viewport check; Phase 7 has no dedicated checkbox for it but carries the identical gap |
| `CoachingInsight.kpiSnapshot.sentToGemini` is only set `true` when `callGemini()` returns successfully — if a real API key is present and the network request is sent but Gemini responds with a late error (e.g. a non-2xx status *after* receiving the request body), `sentToGemini` would still read `false` even though the payload did leave the process over the network. Worth `security`'s attention specifically since Module G's whole audit point is "data leaving the org" — not confirmed as an actual problem even now that a real `GEMINI_API_KEY` and a real successful call exist (this round's live call returned `200` on the first try, so the late-failure branch still hasn't been exercised) | Phase 6 (Module G) → `review/phase-5-6.md` | `security` | No — flagged for the security pass to look at, not a confirmed defect |
| Phase 6's `isStale` "set to `true` by a fresh import touching that period" path was verified by **code inspection only** in the Phase 5/6 round (`import.service.ts:448-479`, unchanged since Phase 2) — the live end-to-end chain (real `POST /import` → `isStale` flips true → `generate` clears it) could not be driven that round because the file-upload action was blocked by that session's auto-mode classifier. The "cleared by generate" half **was** live-verified that round. Not touched by this round's fix (`import.service.ts` is untouched, outside this round's scope) | Phase 6 (Module G) → `review/phase-5-6.md` | `qa-engineer` (re-attempt the full live chain in a future round once file-upload testing is available) | No — code matches the same logic Phase 2's round already inspected; not a suspected defect |
| Phase 6's most recent round (2026-08-16, TARGETED) is now archived (`review/phase-6.md`) — same "TARGETED resets deploy eligibility" gate as the Phase 2/3 row above: needs one more FULL round before `devops` eligibility | Phase 6 (Module G) → `review/phase-6.md` | `qa-engineer` (run one FULL round before deploy) | **Yes** — for `devops` eligibility specifically |
| ~~**Phase 11 (Module O ส่วนที่ 1, Product Master) — `resolveProductViaAlias`'s alias lookup ignores `productTypeId`, so `SalesLine.productTypeId` could end up inconsistent with the resolved `Product.productTypeId`**~~ — **✅ ปิดแล้ว 2026-08-22**: fix (denormalize from the resolved `Product`) ยืนยันอยู่ในโค้ดแล้วและ live-reproduced ซ้ำกับ scenario เดิม ("PC-5" นำเข้าภายใต้ type "C-cae") — ผล: row imported + `WARNING PRODUCT_TYPE_ALIAS_MISMATCH` (ผู้ใช้เลือกเพิ่ม) + `SalesLine.productTypeId == product.productTypeId (GI)` ตรง invariant; ไม่มี raw-column leak | Phase 11 → this round, below / archived FULL round in `review/phase-11.md` | closed (was `backend-engineer`) | No — resolved |
| ~~**Phase 12 fix-round defects (จาก FULL round เช้า 2026-08-24)** — BE1 group derived-target divisor counted only caller-owned territories · BE2 GET /targets hardcoded scope SALESPERSON · BE3 unassigned amounts summed raw Σ SalesLine.total bypassing SalesLineCredit/exclusions · BE4 ambiguity badge had no data source · FE cluster (red build my-territory/page.tsx:34 + lib/types.ts:484, payload field mismatches across the 7 pages, missing bulk-by-province UI, missing group-target editor, DerivedTargetCard unwired + mistyped) · assignments envelope {assignments} vs {territoryAssignments} · GET /territories lacked activeOwnerCount/hospitalCount~~ — **✅ ปิดครบ 2026-08-24 (TARGETED re-check)**: ทุกข้อมีการพิสูจน์สดบน fixtures ปี 2099 (divisor 100,000/100,000 + dedupe control 200,000 · scope default/TERRITORY/TERRITORY_GROUP/400 · proof case 100k→50,000 + bucket deltas เป๊ะ · badge true/false · 7 surfaces diff กับ payload จริง · build เขียว) — ดู Verification Summary | Phase 12 (Module M) → this round below | closed (were backend-engineer/frontend-engineer) | No — resolved |
| **PUT /territory-assignments withdraw flow returns 500** (Phase 12 task ~240): body ที่มี effectiveTo ปิดแถวเดิมแล้ว create แถวใหม่ด้วย effectiveFrom เดิม → ชน @@unique([territoryId, salespersonId, effectiveFrom]) (P2002 หลุดไป error middleware → 500) — ปุ่ม "ถอนผู้ดูแล" ใน territories/page.tsx (closeAssignment) ส่ง shape นี้ทุกครั้ง จึงถอนไม่ได้ทาง UI/API (reproduced live + server log); ฝั่ง "มอบ" ปกติ; **โค้ดส่วนนี้เป็นของเดิมก่อน fix round (git diff ไม่แตะ handler) แต่เพิ่งถูกพบรอบ TARGETED นี้เพราะ fix round แตะไฟล์เดียวกัน** · **SA ruled 2026-08-24** (normative แล้วใน design.md Territory KPI Rules ข้อ 6.2): นอกจาก P2002→409 แล้ว assign branch ต้องมี guard `400` เมื่อ effectiveFrom ใหม่ ≤ effectiveFrom ของแถวที่ยังเปิดอยู่ — ห้ามปิดแถวเดิมด้วยวันก่อนวันเริ่มของมันเอง (ช่วงกลับหัวทำ ACTIVE-filter/activeOwnerCount/viewerScope ลวงทั้งระบบ) · REQUIRED-FIX = guard 1 บรรทัดใน assign branch (precedent: ฝั่งถอนทำไว้แล้ว) — fix surface เดียวกับข้อนี้ | Phase 12 (Module M) → this round below | backend-engineer | **Yes** — ถอนผู้ดูแลใช้ไม่ได้จริง; ต้องแก้ + re-check ก่อนข้อสรุป deploy ใด ๆ ของเฟสนี้ |
| **DerivedTargetCard ยัง unwired** (Phase 12 task ~256): component ถูก retype ตรงสัญญาแล้ว ({source:"MANUAL"|"TERRITORY"|"TERRITORY_GROUP", items:[{territoryId|territoryGroupId, revenueTarget, unassigned?}]}, labels "กรอกเอง"/"คำนวณจากเขต"/"คำนวณจากกลุ่มเขต", amber block) แต่ **ไม่มี page ใด import** และ lib/api.ts#getDerivedTarget ไม่มี caller — ยังไม่มีหน้าจอใดแสดงเป้ารายคนที่ derive ได้เลย | Phase 12 (Module M) → this round below | frontend-engineer | No — non-blocking (Phase 18/F2 จะ reuse) แต่ทำให้ task ~256 ยัง ⚠️ ไม่ tick |
| ~~Bulk-by-province เขียน HospitalTerritoryChange **ต่อแถวที่ match** (แม้ territoryId เดิม) ขณะที่ plan task ~242 เขียน "ต่อแถวที่เปลี่ยน" — deviation เชิงถ้อยคำของสัญญาที่ยังไม่มีใครตัดสิน (audit noise vs ความครบของ trail)~~ — **✅ SA ruled 2026-08-24: implementation เป็นฝ่ายถูก — normative**: audit ต่อแถวที่ action แตะ รวม no-op (from==to) เพราะ action พลิก `territorySource` → MANUAL รายแห่งจริง (verified โค้ด 2026-08-24) · บันทึกใน design.md Territory KPI Rules ข้อ 10 แล้ว · plan ~242 รอ PM แก้ถ้อยคำคราวแตะ plan ถัดไป — ไม่ใช่ defect | Phase 12 (Module M) → this round below | system-analyst | No — observation เดิมจากรอบก่อน ยังเปิดอยู่ |

**Resolved this round (2026-08-16, TARGETED round on Phase 6)**: (1) the **SALESPERSON-can-generate-own-insight permission rule** open item — `business-analyst` closed the underlying business decision (MANAGER-only, salespeople read-only) on 2026-08-16, and this round live-confirmed the corresponding backend/frontend fix (`coachingInsight.controller.ts`, `dashboard/page.tsx`, `reports/individual/page.tsx`, `CoachingInsightPanel.tsx`) enforces it correctly — no longer open. (2) The three leftover `qa-temp-*.ts` scripts in `backend/src/` — confirmed deleted (via `Glob`, zero matches) before this round started; no longer open. (3) The Gemini-success path (real API call, not the fallback) — `GEMINI_API_KEY` is no longer empty in `backend/.env`; this round drove a real `generate` call as MANAGER and got back `status: "SUCCESS"`, `provider: "gemini"`, `model: "gemini-flash-lite-latest"` with real generated Thai coaching text — the previously untestable path is now live-confirmed working, including with the new default model name. No longer open. (4) **"Should the backend refuse to deactivate the last remaining active MANAGER account?"** (previously this file's Phase 1 open item) — `business-analyst` closed this on 2026-08-16 as an Accepted Risk, not a code change: the user explicitly accepts not blocking it (`requirement.md`'s `### ความเสี่ยงที่ผู้ใช้ยอมรับแล้ว (Accepted Risks)` — "ไม่บล็อกการปิดใช้งานบัญชีผู้จัดการคนสุดท้ายที่ยัง active อยู่"). No code was expected or required for this closure; verified only that `requirement.md` actually records it. No longer open.

**Resolved in a prior round** (kept for reference, not re-litigated): Phase 7's SALESPERSON-role live access to `/reports/*` — closed live in the Phase 7 FULL round (`review/phase-7.md`); a leftover orphaned `CoachingInsight` test row (Chidchanok, `YEAR`/2026/0) from a prior interrupted QA attempt on that same phase was found and deleted that round.

Phase 4 introduced no new security-sensitive surface (it computes over already-imported/validated data and gates its one write surface — `PUT/PATCH /settings/*` — behind the existing MANAGER role check; it accepts no untrusted external input and `design.md` doesn't flag Module E `⚠️ Sensitive`), so no new security gate was added for it. Phase 5 (Module F) is the same — read-only, no new write surface, not flagged `⚠️ Sensitive` in `design.md`. **Phase 7 (Module H) is the same again** — read-only reports composed entirely from already-computed/already-validated numbers, no new write surface, `design.md` doesn't flag Module H `⚠️ Sensitive` — none of these three need a `security` pass.

## Verification Summary (current round)

**Phase 12 (โครงสร้างเขต + เป้าระดับเขต, Module M 🔒) — สองรอบในวันที่ 2026-08-24: FULL round (ช่วงเช้า) พบ defect ตามรายการด้านล่าง → engineer fix rounds → TARGETED re-check (รอบปัจจุบัน)** รอบ TARGETED ครอบคลุมของที่แก้ทั้งหมดถึงมาตรฐานเดียวกับ FULL + ทุก task ในเฟสที่แตะไฟล์เดียวกัน + shared-code watchlist + contract check เต็ม + typecheck/lint/build ทั้งโปรเจกต์ + surface sweep ทั้งเฟส

**ความโปร่งใสเรื่องระเบียน**: รายงานของ FULL round เช้าไม่เคยถูกเขียนลงเอกสารเลย (session นั้นเขียน knowledge-tree ถูก guard block) จึงไม่มี Verified File Manifest ฝั่งดิสก์ให้เทียบ — รอบนี้จึงสร้าง manifest ใหม่เองจาก git status/git diff --stat (read-only) + mtime แล้วสแกน/อ่านไฟล์จริงทุกไฟล์ที่ขยับ พร้อมสรุปผลรอบเช้าใหม่จากระเบียนของรอบ ไม่มีจุดไหนเอาคำบอกของ engineer ผ่านโดยไม่พิสูจน์

**ผล FULL round (เช้า — restated)**: schema/contract Module M ตรง design.md ทุกฟิลด์ · Region seed 5 แถว · 409 matrix สองทิศ proven · minor endpoints live-smoked · XOR ✅ · **❌ ที่พบ**: (BE1) divisor ของ group derived-target นับเฉพาะเขตของ caller (BE2) GET /targets hardcode scope SALESPERSON (BE3) ยอด unassigned รวม raw Σ SalesLine.total ข้าม SalesLineCredit/exclusions (BE4) badge กำกวมไม่มี data source (FE) build แดง my-territory/page.tsx:34 + lib/types.ts:484, payload mismatch ทั้ง 7 หน้า, ไม่มี bulk-by-province UI, ไม่มี group-target editor, DerivedTargetCard unwired+contract ผิด · พบเพิ่มระหว่างแก้: envelope {assignments} vs {territoryAssignments} และ GET /territories ขาด activeOwnerCount/hospitalCount

**TARGETED รอบนี้ — สิ่งที่พิสูจน์สดจริง (fixtures ปี 2099, ลบครบหลังเสร็จ, baseline กลับมาเหมือนเดิมทุก count):**

- **BE1 ✅**: group GG={T1,T2} เป้ากลุ่ม 200,000 · A→T1, B→T2 → GET /targets/derived คนละ **100,000** (source:"TERRITORY_GROUP") · control: คนเดียวถือทั้งสองเขตสมาชิก (2 assignment rows / **1 distinct person**) → divisor 1 → **200,000 เต็ม** · กลุ่มไม่มีเจ้าของ (G0 เป้า 50,000): ไม่ตกเข้าใครเลย (sum รวมทุกคน = 277,000 ไม่มีเศษ 50,000 หลุด) · single-TERRITORY path ไม่เปลี่ยน (77,000 keyed territoryId, source:"TERRITORY") · MANUAL override ชนะทั้งก้อน (12,345, source:"MANUAL", items:[])
- **BE2 ✅**: GET /targets?year=2099 default → เฉพาะแถว person-scope (fixture เดียว) · year=2026 default → 5 แถว person-scope พอดี (**grid Phase 3 ไม่กระทบ**) · &scope=TERRITORY/&scope=TERRITORY_GROUP ได้แถวกลุ่มตัวเอง · scope=BOGUS/lowercase → **400** (Zod enum เข้ม + middleware เขียนค่า default กลับ req.query จริง)
- **BE3 ✅**: proof case — ดีล 100,000 แบ่ง credit 50% normal / 50% excluded-flagged → แถวโรงพยาบาลแสดง **50,000** พอดี · top-level unassignedBucket+hospitalCount บวก delta เป๊ะ (+180,000 / +4 จาก pre-fixture snapshot) · **BE4 ✅**: ambiguous=true เมื่อ runner-up ≥30% (60k/40k = 66%), false เมื่อ contributor เดี่ยว, คน excluded ถูกกรองออก**ก่อน** ranking · threshold/predicate นิยาม**ครั้งเดียว**ใน hospitalCreditRanking.util.ts
- **bootstrap equivalence ✅**: dry-run บน DB จริง (141 รพ.) ได้ baseline เดิม **byte-equal ทั้งก่อนสร้าง fixtures (inferred 0 / ambiguousMulti 134 / ambiguousRunnerUp 7) และหลังลบ cleanup แล้ว** — endpoint กับ script ใช้ util เดียวกันจริง
- **envelope/fields ✅**: GET /territory-assignments → {territoryAssignments:[...]} · GET /territories ทุกแถวมี activeOwnerCount+hospitalCount ชัดเจน (ACTIVE-today predicate ตามตัวจาร plan ~239: effectiveTo null หรือ ≥ today — assignment ล่วงหน้านับด้วย, ทดสอบสด) · status filter ACTIVE/INACTIVE ตรง predicate เดียวกัน

- **FE ✅ (7 surfaces diff กับ payload จริง)**: territories table (computed counts, region เป็นป้ายเท่านั้น) · assignment history (.territory.name/.salesperson.displayName/effectiveFrom-To/supervisor) · moves page ใหม่ (single = PATCH /hospitals/:id/territory, bulk-by-province = POST /hospitals/territory/bulk-by-province พร้อม confirm step — bulk execution ไม่รี-รันเพราะ audit trail append-only; live ครั้งสุดท้าย 2026-08-22) · unassigned header/badge fields · targets grid โหลด scope=TERRITORY + save ผ่าน PUT /targets/territory/:territoryId/:year/:month (repointed ✓) · groups manager chips .name + group-target editor prefill/save ครบ (prefill ผ่าน scope=TERRITORY_GROUP, save ผ่าน PUT /targets/group/...) · my-territory tables (mode/territories/soldHospitals/soldBeforeButNotInPeriod fields ตรง payload จริง) · grep ไม่พบ hardcoded role check เหลือนอกจาก canEdit write-gating ที่ plan บังคับ · DerivedTargetCard labels/amber block ตรงสัญญาเป๊ะ — **แต่ยัง unwired (⚠️ ดู Issues Found)**
- **blast radius ✅**: getDerivedTarget ถูก import จุดเดียว (derived endpoint) · upsertScopedTarget paths ครบ + 409 guard รี-proven สด (TERRITORY target บนเดือนที่เป็นสมาชิกกลุ่ม → 409) · hospitalCreditRanking.util.ts import 2 จุดตาม design (endpoint + bootstrap script), ambiguity predicate นิยามครั้งเดียวทั้ง repo · schema.prisma **ไม่ถูกแตะ** (git diff ว่าง) · XOR (assertTargetScopeXor) ครบ 3 call sites ไฟล์ไม่เปลี่ยน
- **gates**: backend lint/typecheck/build ✅ · frontend lint/typecheck/build ✅ · security_scan ✅ · dependency_scan ✅ (**ไม่ใช่ security sign-off** — mechanical เท่านั้น) · **ไม่มี test script ในโปรเจกต์นี้ทั้งสองฝั่ง — verification คือ code inspection + live calls ต่อ requirement.md/design.md เท่านั้น** · FAIL เดียวที่ gate รายงานมาจาก .claude/worktrees/agent-a03c5c2fd41de74c6 (worktree เก่าที่ไม่เคย install dependencies — ไม่ใช่โค้ดโปรเจกต์, pre-existing)

## Verified File Manifest — Phase 12 (2026-08-24 TARGETED round; manifest ของรอบเช้าไม่เคยลงดิสก์ จึงสร้างใหม่ทั้งหมดจากไฟล์จริง)

| File | Bytes | Lines | Round |
|---|---:|---:|---|
| backend/src/services/territory.service.ts | 12011 | 161 | 2026-08-24 (TARGETED re-verify) |
| backend/src/services/hospitalCreditRanking.util.ts (NEW) | 1706 | 36 | 2026-08-24 (TARGETED re-verify) |
| backend/src/controllers/target.controller.ts | 3303 | 97 | 2026-08-24 (TARGETED re-verify) |
| backend/src/controllers/territory.controller.ts | 8391 | 24 | 2026-08-24 (TARGETED re-verify + git diff ราย handler) |
| backend/src/validators/target.validators.ts | 1806 | 52 | 2026-08-24 (TARGETED re-verify) |
| backend/scripts/bootstrapTerritories.ts | 4264 | 112 | 2026-08-24 (TARGETED re-verify + dry-run ×2) |
| backend/src/middleware/validate.ts | 525 | 16 | 2026-08-24 (scope default write-back) |
| backend/src/routes/target.routes.ts | 2392 | 59 | 2026-08-24 (surface sweep) |
| backend/prisma/schema.prisma | 24483 | 730 | 2026-08-24 (contract check + git diff ว่าง) |
| frontend/app/(protected)/territories/page.tsx | 9466 | 97 | 2026-08-24 (TARGETED re-verify) |
| frontend/app/(protected)/territories/moves/page.tsx (NEW) | 10168 | 153 | 2026-08-24 (TARGETED re-verify) |
| frontend/app/(protected)/territories/unassigned/page.tsx | 3776 | 36 | 2026-08-24 (TARGETED re-verify) |
| frontend/app/(protected)/territories/targets/page.tsx | 4278 | 27 | 2026-08-24 (surface sweep) |
| frontend/app/(protected)/my-territory/page.tsx | 8002 | 36 | 2026-08-24 (TARGETED re-verify) |
| frontend/components/territories/DerivedTargetCard.tsx | 2298 | 48 | 2026-08-24 (TARGETED re-verify) |
| frontend/components/territories/TerritoryGroupManager.tsx | 13249 | 207 | 2026-08-24 (TARGETED re-verify) |
| frontend/lib/api.ts | 28131 | 765 | 2026-08-24 (URL/envelope sweep) |
| frontend/lib/types.ts | 26054 | 898 | 2026-08-24 (types vs payload) |

## Per-Task Results — Phase 12 (this round)

- ✅ Verified — `[backend]` XOR assertion/guard (~234): assertTargetScopeXor ครบ 3 write paths, ไฟล์ไม่ถูกแตะรอบแก้, FULL เช้า exercise แล้ว — carried forward ไม่มี drift
- ✅ Verified — `[backend]` group target service (~245, แก้ BE1): divisor = คนไม่ซ้ำเหนือทุกเขตสมาชิกที่มีผลในงวด — พิสูจน์สดครบทั้ง 100,000/100,000 และ control 200,000; zero-owner ไม่รั่วเข้าสรุปใคร; validator คู่กัน (409 รี-proven)
- ✅ Verified — `[backend]` GET /targets/derived (~246): source labels MANUAL/TERRITORY/TERRITORY_GROUP ตรงคำสัญญา, items keyed territoryId/territoryGroupId, MANUAL priority ชนะทั้งก้อน
- ✅ Verified — `[backend]` PUT territory/group target (~247/~248): upsertScopedTarget paths เดิมทั้งระบบ + revision snapshot; 409 guard หลัง refactor
- ❌ Failed — `[backend]` PUT /territory-assignments (~240): **withdraw (effectiveTo) → 500** จาก P2002 ที่ไม่ถูก catch (close-then-create ชน unique ด้วย effectiveFrom เดิม); ปุ่ม "ถอนผู้ดูแล" ของ UI เจอทุกครั้ง; ฝั่ง create ปกติ; **pre-existing** (handler ไม่อยู่ใน diff ของ fix round) แต่เพิ่งถูกพบรอบนี้
- ✅ Verified — `[backend]` GET /territories (~238): activeOwnerCount/hospitalCount ชัดเจน; ACTIVE-today predicate ตามตัวจาร ~239 (รวม effectiveTo อนาคต — ทดสอบสด)
- ✅ Verified — `[backend]` GET /territory-assignments (~239): envelope {territoryAssignments} + status filter
- ✅ Verified — `[backend]` GET /hospitals/unassigned-territory (~243, แก้ BE3/BE4): proof case 50,000 · bucket/count delta เป๊ะ · badge จาก shared ranking util
- ✅ Verified — `[backend]` bootstrap script (~236): ใช้ util เดียวกับ endpoint; dry-run byte-equal baseline ก่อน-หลัง fixtures; idempotent/MANUAL-wins shape คงเดิม
- ⚠️ Carried observation — `[backend]` bulk-by-province (~242): audit **per matched** row ไม่ใช่ per changed row ตามตัวจาร — non-blocking, ส่ง system-analyst; ไม่รี-รันรอบนี้ (append-only audit; live ครั้งสุดท้าย 2026-08-22)
- ✅ Verified — `[frontend]` หน้าจัดการเขต (~251): create/toggle gated MANAGER, region เป็นป้ายเท่านั้น, counts แสดงจริง
- ✅ Verified — `[frontend]` หน้าผู้ดูแลเขต/ประวัติ (~252): มอบ/ถอน (ถอนยิง endpoint เดียวกับที่ ❌ ด้านบน — UI wiring ถูก แต่ endpoint พัง), ตารางประวัติจาก effectiveFrom/To
- ✅ Verified — `[frontend]` หน้าย้ายโรงพยาบาล (~253, NEW): ค้นหา/เลือกย้ายเดี่ยว + bulk ทั้งจังหวัดพร้อม confirm, ยิง endpoint จริง, fields ตรง payload
- ✅ Verified — `[frontend]` หน้ารพ.ยังไม่ผูกเขต (~254): header จำนวน+bucket แสดงตลอด, badge "กำกวม: อันดับ 2 ≥ 30%" จาก server flag, quick-assign wired
- ✅ Verified — `[frontend]` หน้าตั้งเป้าระดับเขต (~255): grid แถวเขต/คอลัมน์เดือน, โหลด scope=TERRITORY, save ผ่าน route territory ที่ repoint แล้ว
- ⚠️ Partial — `[frontend]` DerivedTargetCard (~256): component ตรงสัญญา+label ครบ แต่ **ไม่มี page ใด import / getDerivedTarget ไม่มี caller** — ยังไม่มีหน้าจอแสดงเป้า derive ได้จริง
- ✅ Verified — `[frontend]` หน้าจัดการกลุ่มเขต (~257): membership เต็มเดือน + "สิ้นสุดการเป็นสมาชิก" = PATCH effectiveTo (ไม่มี delete), history แยกจาก chips ที่มีผลในเดือน (.name), 409 แสดง, group-target editor prefill/save ครบ

## Knowledge sync — three-repo mode

**workspace นี้เป็น role: dev — hook ปิดการเขียน `_docs/module/*/plan.md` จาก Target ทุกกรณี ตารางนี้คือคำตัดสินที่ BA-lane session ต้อง copy ลง Status cell ของ plan.md ตรงตามนี้ (ทีละแถว) ไม่ตัดสินใหม่ — กว่าจะ apply งานเหล่านี้ยังไม่ถูกนับว่า done ฝั่ง Knowledge (status.md/traceability/deploy gate):**

| Task id | New Status | Phase |
|---|---|---|
| plan.md ~234 (XOR assertion/guard [backend]) | verified | Phase 12 |
| plan.md ~245 (group target service [backend], BE1 fix) | verified | Phase 12 |
| plan.md ~251 ([frontend] หน้าจัดการเขต) | verified | Phase 12 |
| plan.md ~252 ([frontend] หน้าจัดการผู้ดูแลเขต) | verified | Phase 12 |
| plan.md ~253 ([frontend] หน้าย้ายโรงพยาบาล + bulk-by-province) | verified | Phase 12 |
| plan.md ~254 ([frontend] หน้ารพ.ยังไม่ผูกเขต) | verified | Phase 12 |
| plan.md ~255 ([frontend] หน้าตั้งเป้าระดับเขต) | verified | Phase 12 |
| plan.md ~257 ([frontend] หน้าจัดการกลุ่มเขต amend 2026-08-22) | verified | Phase 12 |
| plan.md ~240 (PUT /territory-assignments — withdraw half broken, 500) | blocked | Phase 12 |

(~256 ไม่อยู่ในตาราง — ยัง ⚠️ Partial คงสถานะเดิมไว้, ไม่ tick)

## Design/requirement contract checks — Phase 12

เทียบ field-by-field กับ Data Model ของ design.md (อ่านเต็มทุกรอบตาม policy): Territory · TerritoryAssignment (shape รอบที่ 3 isSupervisor Boolean, enum TerritoryRole ถูกห้ามและไม่มีใน schema, index [salespersonId, isSupervisor, effectiveTo]) · TerritoryGroup · TerritoryGroupMember (ช่วงเต็มเดือน, triple unique, ไม่มี territoryId @unique) · HospitalTerritoryChange (audit-only) · Target (scope default SALESPERSON, salespersonId nullable, territoryGroupId + triple unique, TargetScope 3 ค่า) · คอลัมน์ที่แตะ Salesperson.excludedFromTerritoryTotals/employmentEndedAt, Hospital.territoryId/territorySource, User.territoryAssignmentsMade — **zero drift** · โปรเจกต์นี้ single-module (_docs/module/ มี folder เดียว) จึงไม่ต้องทำ cross-module ownership lookup · node .claude/scripts/check-schema-contract.js **รันไม่ได้เชิงกลไกใน setup สาม repo นี้** (script หา root เดียวที่มีทั้ง schema.prisma และ _docs/module และไม่ scan backend/prisma) — การเทียบรอบนี้ทำ manual ทั้งสองไฟล์แทน · กฎธุรกิจเทียบกับ Territory KPI Rules ข้อ 2/3/6/6.1/9 + plan: revenue ผ่าน credits เท่านั้น, exclusion mechanism เดียว, นิยาม divisor ทั้งรายเขตและแบบ distinct-person ของกลุ่ม, zero-owner preservation, month-boundary validation, bootstrap 30% threshold แชร์กัน

## Issues Found — Phase 12

1. ❌ → backend-engineer (implementation bug — สัญญาชัดอยู่แล้ว): **withdraw flow ของ PUT /territory-assignments 500 ทุกครั้ง** เมื่อ body มี effectiveTo (close แถวเดิม → create แถวใหม่ด้วย effectiveFrom เดิม → P2002 ที่ @@unique([territoryId, salespersonId, effectiveFrom]) หลุดไป error middleware) — UI "ถอนผู้ดูแล" ส่ง shape นี้เสมอ reproduced live + เห็นใน server log; เป็นโค้ดเดิมก่อน fix round (diff ไม่แตะ handler) แต่ fix round แตะไฟล์เดียวกัน จึงเข้าข่าย blast radius ที่ต้องจับ; วิธีแก้เป็นดุลยพินิจของ engineer (close-only เมื่อ key ซ้ำ / 409 ที่สื่อสารชัด) — requirement/design ("ปิดแถวเดิมด้วย effectiveTo เมื่อถอน") ชัดพอ ไม่ใช่ช่องว่าง design
2. ⚠️ → frontend-engineer: DerivedTargetCard ตรงสัญญาแต่ไม่มีใคร import; getDerivedTarget() ไม่มี caller — ต่อให้ page ที่เหมาะสม (my-territory หรือหน้าเป้า) เพื่อปิด task ~256 ให้ครบ ("+บล็อกแยก ... ในหน้ารวม")
3. Observation → system-analyst (non-blocking): ตัวจาร ~242 "เขียน HospitalTerritoryChange ต่อแถวที่เปลี่ยน" กับ implementation "ต่อแถวที่ match" — ให้เคลียร์ตัวจารว่าต้องการแบบไหน
4. งานข้อมูลค้าง (ผู้ใช้, ไม่ใช่ checkbox): ผู้จัดการสร้างเขต/มอบหมายจริงผ่าน UI → รัน bootstrap จริง — จนกว่านั้นยอด real-period ทั้งหมดยังอยู่ unassignedBucket

## Review Outcome — Phase 12

**Status:** ⚠️ Partial (TARGETED)

ของที่แก้ทั้ง 5 ก้อน (BE1-BE4 + FE cluster/envelope/staffing) **แก้ได้จริงและ re-verify ผ่านหมด** ด้วยการพิสูจน์สด, gates เขียวทั้งโปรเจกต์ แต่ยังไม่ ✅ เพราะ (1) เจอ defect ใหม่ 1 รายการใน blast radius ของรอบนี้เอง — withdraw flow 500 แบบ deterministic รวมถึงปุ่มบน UI (โค้ดเดิม แต่เพิ่งถูกพบ) และ (2) DerivedTargetCard ยัง unwired ทำให้ task ~256 ไม่ปิดเต็ม · **ยังไม่ eligible ให้ devops**: ต้องแก้ withdraw + re-check, รอบล่าสุดเป็น TARGETED จึงต้องมี FULL round ก่อน deploy claim ตาม gate ของไฟล์นี้เอง, 🔒 Security gate บนหัวเฟสยังไม่มี security มาตรวจ, และงานข้อมูลจริง (setup + bootstrap run) ยังค้าง · routing ตาม Issues Found; ผล ⚠️ เป็น hard stop — ให้คนตัดสินทาง

## Unverified Behaviour — undeployed phases

### Phase 13 (Module N) — no automated test suite; rules verified by reading + live calls, with the following residuals

- **ข้อ-5 data-insufficiency label branch never fired live**: this DB's seeded settings carry minMonthsForChurn/minMonthsForConsistency = 1, so `dataCoverageMonths` can't fall below the gate while any real month exists. The label function's output format was verified byte-exact against Territory KPI Rules ข้อ 5 by code inspection ("ข้อมูลยังไม่เพียงพอ (ต้องการ X เดือน ปัจจุบันมี Y เดือน)"); RETENTION/CONSISTENCY's *other* two not-computable branches ("ไม่มีลูกค้าในงวดก่อนหน้า", "ยอดขายเฉลี่ยในช่วงที่พิจารณาเป็น 0") and all computable branches WERE exercised live.
- **Rank-only rendering in a real browser** was not exercised this round (no viewport tooling, same standing gap as earlier phases): the RANK_ONLY payload shape was diffed field-by-field and the components' rendering branches read, but no screenshot-level confirmation. Rank-only rendering also has zero coverage on real periods today because the DB contains no territories/assignments yet (real periods return an empty territories array until the manager does territory setup).
- **Group block on real data**: only fixture-based. Real groups (14M/13M) don't exist in the DB yet.
### Phase 12 (Module M) — ไม่มี test suite; สิ่งที่รอบ TARGETED 2026-08-24 อ่านอย่างเดียว ไม่ได้ execute

- เลข arithmetic ของ group divisor ใน shape อื่น ๆ นอกจากที่ fixture ครอบ (กลุ่ม >2 เขตสมาชิก, หลายเจ้าของปนกัน, boundary กลางเดือน) — territory.service.getDerivedTarget
- Bootstrap inference branch (set territorySource=INFERRED) **ยังไม่เคย run จริงเลยแม้แต่ครั้งเดียว** — ทุกรอบเป็น dry-run บนสถานะ ownerless (inferred 0); run จริงรอ setup ของผู้จัดการ
- Branch `unassigned:true` ใน getDerivedTarget unreachable ผ่าน endpoint ตามที่เขียน (เขตจะเข้า payload ได้ต้องมาจาก assignment ของ caller ซึ่งทำให้ caller นับเป็น owner เสมอ) — invariant ที่สำคัญของสัญญา ("เป้าของ unit ไม่มีเจ้าของ ห้ามหารเข้าใคร") ยืนยันแล้วว่า HOLDS เชิงโครงสร้าง แต่ปัจจุบันไม่มี consumer ไหนรับ/แสดง block ที่ preserve ไว้ได้
- activeOwnerCount บน GET /territories นับ assignment อนาคต (effectiveFrom ในอนาคต) เป็นผู้ดูแล — ตรงตัวจาร ACTIVE ของ plan ~239 แต่อ่านขัดกับคำว่า "ผู้ดูแลปัจจุบัน"; บันทึกไว้เป็นประเด็นให้ SA เคลียร์ตัวจารได้ ไม่ใช่ defect claim
- bulk-by-province และ PATCH /hospitals/:id/territory ไม่ได้รี-execute รอบนี้ (audit trail append-only) — live ครั้งสุดท้าย 2026-08-22
- Responsive rendering ของทั้ง 7 หน้า Phase 12 ที่ viewport มือถือ — standing gap เดิม ดู Open Issues


## Archived rounds

- Phase 1 (Auth & User Management, Module B) — accepted, complete (16/16), historical (predates FULL/TARGETED mode labeling) → `review/phase-1.md`
- Phase 2 (Excel Import & Master Data, Module C) — accepted, complete (19/19), most recent round TARGETED → `review/phase-2.md`
- Phase 3 (Targets, Module D) — accepted, complete (10/10), most recent round TARGETED → `review/phase-3.md`
- Phase 4 (KPI & Scoring Engine, Module E) — accepted, complete (18/18), FULL round, zero defects → `review/phase-4.md`
- Phase 5 (Dashboards & Leaderboard, Module F) — FULL round, 10/11 (1 non-blocking partial, responsive), pending user decision → `review/phase-5-6.md`
- Phase 6 (AI Coaching Insights, Module G) — FULL round (2026-08-15), 14/14, zero defects → `review/phase-5-6.md`; superseded by a TARGETED round (2026-08-16) that fixed and confirmed the MANAGER-only generate permission → `review/phase-6.md`; pending user decision, required `security` pass, and one more FULL round before `devops` eligibility
- Phase 7 (Coaching Reports & Export, Module H) — FULL round, 8/8, zero defects, pending user decision → `review/phase-7.md`
- Phase 8 (ซ่อมข้อมูลชื่อซ้ำ และการแบ่งเครดิตดีล, Module J 🔒) — original checklist FULL round (2026-08-22, 15 tasks) + SalesmanNameReview amend round, zero blocking defects; skippedRows observation stays open → `review/phase-8.md`
- Phase 11 (ทะเบียนสินค้า, Module O ส่วนที่ 1) — FULL round (2026-08-19), 5/7 ✅ + 1 ❌ (import-service bug, closed 2026-08-22) + 1 not-started → `review/phase-11.md`
- Phase 12 (โครงสร้างเขต + เป้าระดับเขต, Module M 🔒) — backend verified 2026-08-22 (migration/schema contract round-4, live smoke incl. both 409 guard directions, bootstrap gap fixed in-round); frontend ×7 pages + real bootstrap run still outstanding → `review/phase-12.md`
- Phase 13 (KPI รายเขต และรายงาน KPI รายเขต, Module N 🔒) — FULL round 2026-08-24, 18/18 ✅ zero defects → review/phase-13.md (Unverified Behaviour block ยังค้างใน review.md จนกว่าจะ deploy)

## Change Log

- 2026-08-24 — **Phase 12 (Module M): FULL round (เช้า) → engineer fix rounds → TARGETED re-check (session นี้).** FULL เช้าพบ defect 5 ก้อน (BE1 group-divisor นับเฉพาะเขตของ caller · BE2 GET /targets hardcode scope · BE3 ยอด unassigned ข้าม SalesLineCredit/exclusions · BE4 badge ไม่มี data source · FE cluster: build แดง, payload mismatch 7 หน้า, ไม่มี bulk UI/group-target editor, DerivedTargetCard unwired/mistyped; พบเพิ่มระหว่างแก้: envelope {assignments}→{territoryAssignments} + GET /territories ขาด activeOwnerCount/hospitalCount) — write-up ของรอบนั้นไม่เคยลงเอกสาร (guard-blocked session) รายงานจึง restate ใน Verification Summary · **TARGETED รอบนี้พิสูจน์ทุก fix สดบน fixtures ปี 2099 ผ่านหมด** (รายละเอียดเต็มใน Verification Summary), blast radius สะอาด, contract check zero-drift, gates เขียวทั้งโปรเจกต์ · **เจอ defect ใหม่ 1 รายการ (pre-existing)**: PUT /territory-assignments withdraw → 500 (P2002 ไม่ถูก catch; ปุ่ม "ถอนผู้ดูแล" ของ UI เจอทุกครั้ง) ส่งกลับ backend-engineer · DerivedTargetCard ยัง unwired (⚠️ ~256) ส่งกลับ frontend-engineer · plan.md Status เปลี่ยน 9 แถว บันทึกใน "Knowledge sync — three-repo mode" (8 verified / 1 blocked) — BA-lane session ต้อง apply · fixtures ลบหมด baseline counts ตรงทุกตัว bootstrap dry-run byte-equal ก่อน-หลัง · Phase 13's FULL round archive ลง review/phase-13.md verbatim

- 2026-08-24 — Phase 13 (Module N) FULL verification round, 18/18 ✅ zero defects — full entry moved verbatim to `review/phase-13.md`.
- 2026-08-22 — **Verification round: Phase 8 amend (SalesmanNameReview queue — first verification for these tasks), Phase 2 amend (`employmentEndedAt` editor), and Phase 11 fix+WARNING re-check.** Live-tested end to end with real imports against the real dev server/DB: queue creation on auto-create, KEPT_SEPARATE + re-decide `409`, MERGED moving lines/credits + deleting the duplicate + MERGED-memory resolving later imports without re-asking, edge cases (self-point 400 / nonexistent target 404), role gates (401/403 per endpoint incl. a throwaway active SALESPERSON account, deleted after), `employmentEndedAt` partial-update semantics, and the exact cross-type bug scenario from the archived FULL round now clean with `PRODUCT_TYPE_ALIAS_MISMATCH`. Schema contract matched field-by-field; migration applied to the real DB; all test data removed (10 salespeople / 0 reviews at close); whole-project typecheck/lint/build green. `plan.md`: Phase 8 amend tasks ×4, Phase 2 amend tasks ×2, Phase 11 lines 204/208 marked `[x]`. Previous Phase 11 FULL round archived verbatim to `review/phase-11.md`; skippedRows Open Issue updated to implementation-only.
- 2026-08-22 — **Phase 12 backend round (Module M) — first verification for the phase's backend.** Contract-exact migration/schema (round-4), Region seed, Target scope-guards, live endpoint smoke (territories/assignments/groups/members/derived) with both 409 guard directions proven, bootstrap gap fixed in-round (`scripts/bootstrapTerritories.ts`, dry-run proven over 141 hospitals). Remaining uncovered: frontend ×7 + minor endpoints (PATCH territory, GET assignments list, hospital-territory PATCH/bulk/unassigned, group-target upsert live, product-groups note). Full write-up in Verification Summary above.
- 2026-08-22 — **Phase 8 FULL round over the original checklist (first verification for these tasks, same session).** All 15 original tasks verified and marked `[x]`: Module J schema/migration contract-exact; backfill invariants hold on the real dataset (0 `SalesLine` without credit, Σ=100 everywhere, credited total = company total exactly at 14,555,965.12); normalizer single-source; shared-deal import proven both ways live (new combination of known people → auto rule 50/50 + `SHARED_CREDIT_RULE_CREATED`; unknown member → `UNKNOWN_SALESMAN_IN_SHARED_DEAL` skip without crediting the resolvable one); hospital thaiCore-match/latinCore-diff → NEW_HOSPITAL **plus** PENDING review queued, never auto-merged; GET/PATCH endpoints live-tested incl. share edit (Σ≠100 → 400) and KEPT_SEPARATE decision; cleanup script revert flag inspected. Non-blocking observation for `system-analyst`: all-unseen members in a shared deal error-skip rather than auto-create (item-5 letter) — UX revisit candidate.
- 2026-08-22 — **`security` pass completed — Modules B/C/G required gates all ✅ passed** (`security.md`): 0 Critical/Important open. Findings: F-B1 login rate limiting (Medium 🔵), F-C1 workbook size guard (Low 🔵), F-G1 key-in-URL (Low 🔵), F-G2 sentToGemini semantics (Low 🔵, carried from 2026-08-15). Baseline verified solid: bcrypt(10) + per-request DB check (deactivation = instant revocation), JWT env secret + expiry, upload caps/filters + advisory lock + parameterized queries, key server-side only with anonymization applied at call site.

- 2026-08-15 — First verification round for Phase 1 (Module B). 15/16 ✅ Verified. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — Second verification round for Phase 1 (Module B). 16/16 ✅ Verified, Phase 1 complete. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — First verification round for Phase 2 (Module C). 18/19 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-2.md`.
- 2026-08-15 — First verification round for Phase 3 (Module D). 9/10 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-3.md`.
- 2026-08-15 — TARGETED re-verification of two previously-flagged fixes: Phase 2's `ImportBatch` failure-path persistence and Phase 3's missing `GET /product-types` (both fixed, confirmed live). 19/19 Phase 2 and 10/10 Phase 3 tasks now ✅ Verified. Full entry archived to `review/phase-2.md`/`review/phase-3.md`.
- 2026-08-15 — FULL verification round for Phase 4 (Module E, KPI & Scoring Engine). 18/18 tasks ✅ Verified, zero defects, all `plan.md` boxes checked. Full entry archived to `review/phase-4.md`.
- 2026-08-15 — FULL verification round for Phase 5 (Module F, Dashboards & Leaderboard) and Phase 6 (Module G, AI Coaching Insights), run together since both build directly on Phase 4 and were implemented in the same session. Phase 5: 10/11 ✅ Verified, 1 ⚠️ Partial (responsive layout not visually confirmed at mobile viewport — no browser tool available; code pattern is sound). Phase 6: 14/14 ✅ Verified, zero implementation defects. Full entry archived to `review/phase-5-6.md` (superseded by this round).
- 2026-08-15 — FULL verification round for Phase 7 (Module H, Coaching Reports & Export) — the module's last implementation phase. 8/8 tasks ✅ Verified, zero implementation defects, all `plan.md` boxes checked. Checked first whether a previously-interrupted QA attempt on this exact phase had left any write-up or checkboxes (it hadn't — only two leftover artifacts: 3 reusable `qa-temp-*.ts` scripts and one orphaned test `CoachingInsight` row, the latter deleted this round). Independently recomputed the individual report's numbers for Chidchanok Seethong Q1 2026 against Phase 4/6's already-verified figures (exact match, reproduced a third time inside a freshly-generated `CoachingInsight`); verified the team-overview ascending sort against real data, cross-checked independently against `GET /kpi/team` (exact match on all 12 scores); downloaded and programmatically parsed both real `.xlsx` exports (not just checked HTTP status), confirming row-for-row content matching what's on screen; closed the previously-flagged SALESPERSON-role access gap live (200 across all 3 report endpoints for both an own-linked and a fully-unlinked SALESPERSON token, 401 unauthenticated); confirmed zero duplicate KPI/insight calculation logic (report layer only composes Phase 4/6's services); confirmed the previous-period comparison has no off-by-one (Q1 2026 → Q4 2025, via the same unchanged `period.util.ts` function already used elsewhere). Both `backend`/`frontend` `typecheck`/`lint`/`build` clean. Restructured `review.md`: archived Phase 5/6's full round verbatim to `review/phase-5-6.md`, carried forward every still-open item, added a new open item for the 3 leftover QA temp scripts, and added Phase 7 as the new current round. All 7 module phases now have at least one completed verification round — see the full-module summary delivered alongside this round for what "ready for `devops`" still requires.
- 2026-08-16 — TARGETED verification round for Phase 6 (Module G, AI Coaching Insights), re-checking the uncommitted fix that makes `generateInsight` MANAGER-only (closing the previously-open business-decision item, per `business-analyst`'s 2026-08-16 decision) plus its blast radius (both frontend pages, the shared coaching panel component, an unrelated Gemini model-name change). Live-tested all 3 permission cases (MANAGER `201`, SALESPERSON-even-own `403`, unauthenticated `401`) against the real dev DB; confirmed the shared-code watchlist (`authenticate.ts`, `prisma.ts`, `lib/api.ts`, the protected layout) and the full `CoachingInsight` schema contract show zero drift; whole-project `typecheck`/`lint`/`build` clean on both `backend`/`frontend`. Also live-tested the Gemini-success path for the first time in any round (a real `GEMINI_API_KEY` is now present) — real call succeeded with the new `gemini-flash-lite-latest` default model, closing that previously-open item too. Also closed, by inspection of `requirement.md`, a separate previously-open item (blocking deactivation of the last remaining active MANAGER) that `business-analyst` resolved as an Accepted Risk requiring no code. Confirmed the 3 leftover `qa-temp-*.ts` scripts are gone. Archived Phase 7's FULL round verbatim to `review/phase-7.md`; this TARGETED round is now the current round. Per this file's own gate, Phase 6's deploy eligibility resets to "needs one more FULL round" since its most recent round is now TARGETED — new Open Issues row added for this.
- 2026-09-11 — **FULL System Verification Pass (Phases 0–21, All 100% Green).** Re-platformed ASP.NET Core 9 + EF Core + Next.js 16 stack fully verified across all components: 89 Domain unit tests, 1 Application test, 144 API integration tests passing (234 total), 0 compiler warnings/errors on backend, 0 TypeScript errors (`npm run typecheck`), 0 ESLint warnings (`npm run lint`), and 33/33 Next.js pages statically built via Turbopack (`npm run build`). Verified new Phase 21 additions: `SUPERVISOR` role and scope resolver, `SalesmanAlias` entity and 3-way dry-run import execution (`AUTO_CREATE`, `MAP_EXISTING`, `SKIP`), multi-sheet individual reports export with formula injection sanitization, and WCAG 2.2 AA accessibility (font >= 16px, contrast >= 4.5:1, touch target >= 44px, focus rings). Verified Phase 19 archive recovery endpoint (`POST /import-batches/{id}/restore`), Phase 13 territory overview JSON endpoint, and Phase 9 hospital registry bed count parsing & uncontactable hospitals query. All gates passed.

